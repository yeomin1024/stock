# =============================================================================
#  run_pipeline.py
#  VERSION: v1.19.1 - 2026-09-15 - [ROUND63 — S v0.53.0 · I v0.24.0] 00A 판정·분해·격자 3블록.
#    사용자 지시: "예측 수익이 buy and hold 보다 훨씬 많이 나와야해 지금 보니까 그런게 없어
#    다시 섹터랑 산업부터 문제 찾아서 개선방법 찾아서 수정해 2개만" ⇒ S·I만 수정(K 무변경).
#    ★★★ 실측 결론: 전략은 **이미** B&H를 복리 **7.90배**(1316.6% vs 166.7%) · 칼마 **11.4배**
#      (3.74 vs 0.33)로 이기고 있었다. 시트가 '몇 배 이겼나'를 계산해 주지 않아 안 보였을 뿐이다.
#      ⇒ 00A 블록 B에 **★★★ 판정 행** 신설(n/4 승 · 배수 · CAGR · MDD · 칼마 한 문장).
#    ★★ '단순합'으로 보면 예측이 지는 것은 **수학적 구조**다 — w ≤ 1이면 Σ(w·r) − Σ(r) = −Σ((1−w)·r)
#      이므로 빼 둔 날의 수익 합이 음수일 때만 단순합에서 이긴다. 복리로 번 것은 반영되지 않는다.
#      실측: 단순합 2/11 승 ↔ **복리 7/11 · MDD 11/11**. 시트가 그 이유를 직접 설명한다.
#    ★★ 블록 D(회피일 분해): 격차(회피−보유)가 음수인 섹터 **11/11** ⇒ **예측은 실제로 작동한다.**
#      ②−①가 음수인 원인은 **부분보유일**(w=0.5)에서 상승을 절반만 먹는 것 = 상위 계층 예산 문제.
#    ★★ 블록 E(노출 상향 격자 · 진단만): 게이트 통과일을 1.00으로 올리면 복리 +26%인데
#      MDD 2.5배 악화 · 칼마 3.74 → 1.66 · **한계비율 0.241**. 리스크 파라미터이므로 라이브 무변경.
#    ⚠ 측정으로 닫힌 방향 3종: 비중 하한 상향 · 확신 비례 비중 · WF 예측성적 재배분 — 전부 나빠진다.
#      ⇒ 이 계층의 수익은 **모멘텀 집중**에서, 예측은 **하락 회피**로 기여한다. 섞으면 둘 다 잃는다.
#
#  VERSION: v1.19.0 - 2026-09-15 - [구버전 실행 방지 가드 · 기능 체크리스트] REPORT62.
#    R61에서 코드는 고쳤는데 노트북이 구버전을 wget해 새 시트가 없었다. _MIN 표 + ⚠⚠⚠ 경고 신설.
#
#  VERSION: v1.20.0 - 2026-09-16 - [ROUND66 — S v0.56.0] 구버전 가드 최소버전 상향 + 신규 기능 2건 확인.
#    · _MIN의 S 최소버전 v0.55.0 → **v0.56.0**.
#    · 기능 체크리스트에 **★E_t바닥·퇴출(라이브)**(market_exposure_floor)과 **★00B_곡선그래프**를 추가 —
#      사용자가 "뭘 고쳤는지도 모르겠고 수치가 그대로인데"라고 지적한 R64 사고(구버전 파일로 실행)의
#      재발 방지. 갱신 확인 문구도 "2번째 탭이 00B_곡선그래프 · 22_시장예산바닥판정 존재"로 바꿨다.
#
#  VERSION: v1.18.0 - 2026-09-15 - [ROUND61 — S v0.51.0 · I v0.22.0 · K v0.3.0] 00A 시트 · 배분층 · 다음날.
#    사용자 지시 넷: (1) "각각 맨 앞에 시트 새로 하나 생성해서 각 섹터, 산업, 주식별 buy and hold시
#    수익률(이 때 수익률은 복리가 아닌 변동률 합산)과 단독 예측으로 거래 시 수익률 비교하고 전략 수익률도
#    같이 표시해" (2) "다음날 예측이 빠지는데 하도록 수정하고" (3) ★★ "배분전략 거래 할거면 전체자산을
#    1로 해서 그걸 배분해서 각 총합이 1이되도록 하라고 왜 자꾸 각 티커별로 비중이 1이냐고"
#    (4) 하락회피·상승참여 판정 후 원인 개선
#
#    ── ★★★ (3)의 지적이 정확했다 — K 계층이 28배 레버리지였다 ─────────────────────────
#      stock 01Z 실측: 목표비중 합계 **평균 26.46 · 최대 28.0**(28종목 × 각 1.0). 배분층이 없었다.
#      S·I는 문제없었다(S 배분합계 최대 1.0002=부동소수 잔차 · I 산업배분 합계 최대 0.95 · 초과일 0).
#      ⇒ K v0.3.0에 **build_allocation() 신설**. 라이브 `equal_fixed` = 종목마다 1/N 고정 슬리브,
#        단독 신호가 감축이면 그 슬리브는 **현금**. 합계가 1을 넘으면 **코드가 정규화**한다.
#      ⇒ **[배분격자] 6행 + 동일가중 B&H 기준선** 신설. 이 runner가 **I의 산업 비중을 K에 넘겨**
#        '★ I계층 산업비중 연동(4계층 정합)' 행을 살린다 — 4계층 설계가 처음으로 실제로 연결된다.
#
#    ── (1) 00A_수익비교: 세 리포트 **맨 앞**에 같은 꼴로 ─────────────────────────────────
#      ① B&H **단순합**(Σr · 복리 아님, 지시대로) ② 단독예측 단순합 ③ 전략기여 단순합 + 복리 병기.
#      블록 B는 **전체자산 1.0** 기준 포트 비교 + **비중 합계 감사** + **예산 사용률**(상위 계층 대비).
#
#    ── ★★ (4)의 답 — 상승 미참여의 천장은 S가 아니라 M이다 ────────────────────────────
#      S 13c 실측: **배분합계 < E_t 인 날이 0일(0%)** — 섹터층은 M 예산을 한 번도 덜 쓰지 않았다.
#      2020-10~2021-09 최대 강세장 211일에 **E_t 평균 0.439 · E_t=1.0인 날 2%**(벤치대비 −14.82%p).
#      2020-03 코로나 저점 5일은 **E_t=0.000 전 구간**(−17.36%p) — 섹터층은 쓸 예산이 아예 없었다.
#      13n 교차검증: 큰상승일 상위5% 110일에 섹터 합 128.03% vs M 합 105.89% = **+22.14%p**,
#        초과수익 연율 **+8.43%p** · IR **1.271**. ⇒ S는 이미 자기 몫을 한다.
#      ⇒ S/I를 더 고쳐도 상승 참여는 안 올라간다. M의 E_t를 손대야 하고 그것은 사용자가 지정한
#        이번 범위("sector, industry, stock") 밖이다. **그래서 고치는 대신 00A가 매 실행
#        '예산 사용률'을 찍게 했다** — 그러지 않으면 다음 라운드에도 못 올라갈 벽을 밀게 된다.
#
#    ── (2) 다음날 예측 ────────────────────────────────────────────────────────────
#      S·I는 이미 있었다(01Z 구분='예측' 행 + 00시트 '다음 거래일 예측' 블록). **K만 없었다** →
#      01Z 맨 끝에 구분='예측(다음 거래일 집행)' 행 추가. t일 확정 → t+1일 집행 규칙의 명시다.
#
#    [실행] 변경 없음 — main(run_stock_layer=True)가 기본. 되돌리기:
#      K 배분층 끄기(합계 항상 1.0 · 방어 없음): k_overrides={"STOCK_ALLOC_MODE": "equal_eligible"}
#      K 배분격자 끄기: k_overrides={"ALLOC_GRID": ()}
#      I 국면 제약 복원: i_overrides={"INDUSTRY_LEADER_REGIMES": ("RISK_ON",)}
#      00A 시트는 항상 나온다(끄는 스위치를 두지 않았다 — 사용자가 맨 앞에 요구한 시트다).
#
#  VERSION: v1.17.0 - 2026-09-15 - [ROUND60 — S v0.50.0 · I v0.21.0 · K v0.2.1] 하락회피·상승참여 판정.
#    사용자 지시: "결과인데 각각 문제 찾아서 최대한 개선해 buy and hold랑 비교했을 때 하락 기간을 피했는지
#    (비중 최대로 감축) 상승을 타서 제대로 수익을 최대한 많이 냈는지(비중 최대로) 판단해서 그렇지 못한
#    기간에서 문제 원인 찾아내서 개선해"
#
#    ★ 이 라운드의 핵심은 **판정 단위를 고친 것**이다. v0.49/v0.20의 19 시트(블록 A)는 "그 **자산**의
#      상승을 탔나"를 묻는다. 로테이션 계층은 하루에 K개만 들므로 나머지는 **정의상** 미참여가 되고
#      참여율이 0 근처로 찍힌다 — 전략의 고장이 아니라 **질문의 단위가 틀린 것**이다.
#      사용자가 묻는 것은 "**B&H 대비** 이 계층이 하락을 피했고 상승을 탔나" = **포트폴리오 단위**다.
#    ⇒ 세 계층 모두 19 시트에 **블록 C — 포트폴리오 구간**이 생겼다. 요약 3행(하락/상승/★★ 종합)이
#      "방어형인가 공격형인가"를 부호로 답한다. 구간 분할은 사후이고 목표비중에 들어가지 않는다(룩어헤드 아님).
#
#    ── 새로 볼 것(리포트 3종의 19 시트 블록 C · 06 시트) ─────────────────────────────────
#      S(sector_rotation_report.xlsx) 19 → 블록 C가 **벤치 2종**(SPY B&H · 11섹터 균등 B&H)으로 2세트
#      I(industry_rotation_report.xlsx) 19 → 블록 C가 벤치 2종(S★ 산업 미사용 · SPY B&H)으로 2세트
#      K(stock_regime_report.xlsx) 19 → 블록 C 1세트(29종목 동일가중 B&H) + 06에 **★ 포트(동일가중) 행**
#
#    ── K 계층: 출하 전에 회귀 테스트가 내 설계를 반증했다(v0.2.0 → v0.2.1) ─────────────────
#      v0.2.0은 감축 규칙 3종을 OR로 묶었는데, 그 중 둘이 "자기이력 **하위 1/3**" 분위 버킷이라
#      **정의상** 각각 33%의 날에 점화된다 → 합집합 60% → 평균노출 **0.4942**.
#      대체하려던 ext200(0.5866)보다 **더 낮았다**. 사용자 요구("비중 최대로")와 **반대 방향**이었다.
#      ⇒ v0.2.1: 라이브는 **E3(어닝 발표 후 21일 & 서프라이즈<0) 하나**. 이벤트 모양이고 정밀−기저
#        +0.0481로 측정된 것 중 최고다. 합성 실측 평균노출 0.8075(ext200 0.6900 대비 +0.1175).
#        P2·E2는 퇴출이 아니라 **[노출격자]로 내렸다** — 판정은 내 계산이 아니라 엔진 시트가 한다.
#      ⇒ 신규 `CUT_MIN_AGREE`(동의 개수 · S의 ROTATION_MIN_AGREE와 같은 장치) ·
#        `CUT_EXPOSURE_FLOOR_WARN=0.70`(하한 미달 시 **경고만** — 리스크 파라미터를 조용히 안 바꾼다) ·
#        `EARNINGS_LIMIT 60 → 100`(K1 어닝 기반 EPS YoY의 측정 구간을 25년으로) ·
#        06 시트에 **★ 포트(동일가중) 행**(종목별 중위와 포트 칼마는 다른 값이다 — 둘 다 싣는다).
#      ⚠ v0.2.0의 06 시트 버그도 고쳤다: 격자 모든 행이 **라이브 노출**을 찍고 있었다(격자의 핵심
#        지표가 노출인데 전부 같은 값이었다). 이제 행마다 자기 노출을 찍는다.
#
#    [실행] 변경 없음 — main(run_stock_layer=True)가 기본이다. 되돌리기:
#      I: i_overrides={"INDUSTRY_LEADER_REGIMES": ("RISK_ON",)}   (v0.20.0 동작)
#      K: k_overrides={"LIVE_RULE": "ext200"}                      (v0.1.0 동작)
#      K: k_overrides={"CUT_RULES": ("E3 발표 후 21일 이내 & 서프라이즈<0",
#                                    "P2 vol21 자기이력 하위1/3",
#                                    "E2 서프라이즈 자기이력 하위1/3")}   (v0.2.0 동작 — 권하지 않는다)
#      셋 다: 19 시트 끄기 = {"SHOW_UPDOWN_SEGMENTS": False}
#
#  VERSION: v1.16.1 - 2026-09-15 - [배너에 K.VERSION 추가 · stock_regime.py 사전 점검] 사용자 보고 대응.
#    증상: 실행 배너가 "M.VERSION … | S.VERSION … | I.VERSION … | runner: v1.16.0"으로만 찍혀
#      **주식 계층이 파이프라인에 있는지 알 수 없었다**(사용자 질문: "stock regime도 실행되는거야?").
#    원인 두 개:
#      (1) v1.16.0은 K를 **맨 끝에서** 로드하고 배너에 넣지 않았다 → 배너만 보면 K가 없는 것처럼 보인다.
#      (2) stock_regime.py를 노트북 상단 wget 목록에 안 넣었으면 load_module이 FileNotFoundError를 내고
#          try/except가 삼켜서 건너뛴다 — 그것도 **M·S·I를 다 돌린 20분 뒤**에야 알게 된다.
#    수정: K를 M·S·I와 **같은 자리에서 로드**해 배너에 `K.VERSION`을 찍고, 파일이 없으면 시작 직후
#      한 줄로 "wget 목록에 추가하세요"까지 알린다. 의도적으로 끈 경우(run_stock_layer=False)도 구분해 찍는다.
#      ⚠ 로드 위치만 앞으로 옮겼고 **실행 순서는 그대로**다 — K의 다운로드·리포트는 여전히 M·S·I 리포트가
#      끝난 뒤 마지막이다(K가 실패해도 앞 세 리포트는 이미 파일로 남아 있어야 하므로).
#    확인법: 배너에 `K.VERSION: v0.1.0`이 보이고, 실행 끝의 "생성 완료:" 줄에 stock_regime_report.xlsx가
#      네 번째로 들어오며 그 아래 파일 크기 목록에도 나온다.
#
#  VERSION: v1.16.0 - 2026-09-15 - [문서 + 실행 레시피] **I v0.19.0 → v0.20.0** · ★ 신규 4번째 계층
#                    **stock_regime.py v0.1.0(K) 실행 추가** · S v0.48.0 · M v1.53.1 무변경.
#
#  사용자 지시(REPORT59): "국면 판단 엑셀처럼 섹터별, 산업별 상승하락구간 시트 만들어서 어디가 문제인지
#  판단하도록 수정해 (…) sector, industry regime 이렇게 2개 고치는 거야 그리고 이제 가장 마지막 층인
#  개별 주식도 똑같이 예측하도록 해 일단 샘플로 각 산업별 대표 티커 하나씩하고 펀더멘탈, 어닝 같은것도 보도록해"
#  사용자 확인: 30MB는 **리포트 파일 전체** 제약 · 주식 계층은 **신규 파일** · 데이터는 **yfinance**.
#  ⚠ S(sector_rotation.py) 수정은 **사용자가 v0.48.0을 업로드한 뒤** 같은 19 시트를 이식한다
#    (작업 폴더에는 낡은 v0.47.0만 있어 그 위에 작업하면 v0.48.0의 L1·L4 변경분이 날아간다).
#
#  ★★ 리포트18 판정 — v0.19.0 [검증] ⑨ (a)(b)(e) PASS · **(c)(d) FAIL**
#    (a) 칼마 **3.568** ≥ 3.479 ✔  (b) MDD **−0.1036**(개선) ✔  (e) NEUTRAL 3.770 vs 달력 3.740 ✔(간신히)
#    X1(CAP_HOLD 0)은 예측과 거의 일치(0.3695 / −0.1036 / 3.568 vs 예측 0.3699 / −0.1036 / 3.571).
#    ★ **13f ② MDD 악화 기준이 처음으로 PASS**(0.0081 ≤ 0.010) — 이제 ①②③ PASS, ④⑤만 FAIL.
#    총노출: 최대 0.9500 · **0.9 이상 102일** · 0.5 이상 114일.
#    (c)(d): 13p A5에서 ① 전 산업 +0.1402(연도 6/7) vs ② WF 완화 +0.0893(2/3) · **대조 알파벳 6개 +0.1205** ·
#      알파벳 3개 **+0.1358(7/7)**. 같은 개수를 알파벳으로 골라도 게이트보다 낫다 → 산업별 선택은 값이 없다.
#    ★ 그리고 격자가 더 큰 것을 잡았다 — **강등 두 개가 이제 손해**:
#      반증(강등 둘 다 없음) 0.3749 / −0.1036 / **3.620** / 한계비율 **2.000**
#      추세가드만            0.3696 / −0.1036 / 3.568
#      라이브(둘 다)         0.3695 / −0.1036 / 3.568
#      MDD가 같은데 반증이 CAGR +0.54%p·칼마 +0.052 높다. v0.18.0 ⑧(e)가 그대로 발동한다.
#      원인: **X1(보유 0)이 이미 그 위험을 제거했다** — 두 위험 축소 장치가 겹치면 수익만 깎인다
#      ([국면확신캡격자] NEUTRAL×래더 3.770 < 평탄캡 시절 3.787도 같은 현상).
#
#  ── I v0.20.0 변경분 ─────────────────────────────────────────────────────────────────
#    (Y1) ★ 되돌림 3건(사전등록 규칙 발동): CAP_WARN_WF_GATE·CAP_WARN_DOWNGRADE·CAP_TREND_GUARD → False.
#         라이브 = **단순 확신 래더**(확신 통과 CAP 1.0 / 보유 중 미달 0.0).
#         기대: 리포트18 '강등 둘 다 없음' 행과 일치 — CAGR 0.3749 · MDD −0.1036 · 칼마 3.620.
#    (Y2) [확신캡격자] 6행 — 강등 **4조합(없음/경고만/추세만/둘 다)** 이 처음 다 실린다. 리포트18에는
#         '경고강등만'이 없어서 그 단독 효과가 측정되지 않았다.
#    (Y3) ★ 신규 시트 **19_상승하락구간** — 사용자 지시의 본체. 지그재그(고점→저점/저점→고점, 최소 7%)로
#         구간을 나누고 그 구간에서 **그 산업의 비중이 어떻게 움직였는지**를 붙인다.
#         블록 A 29행 = "어디가 문제인지" 요약(하락 방어 벤치대비 합 · 감축 성공률 · 상승 참여 벤치대비 합 ·
#           참여율 · **문제 유형 판정**) / 블록 B = 구간 상세(M 05b와 같은 열 + 벤치대비).
#         ★ 벤치는 **도달 가능한 것**이어야 한다 — 상승구간은 '부모 섹터 비중을 그 산업에 전부' 대비,
#           하락구간은 무포지션(0) 대비. (첫 구현에서 '비중 1.0 완전참여'를 벤치로 뒀다가 산업 한 칸은
#           절대 닿을 수 없어 참여 부족이 구조적으로 거대해지는 결함을 발견해 고쳤다.)
#
#  ── ★ 신규 파일 stock_regime.py v0.1.0 (K = 4번째 계층) ────────────────────────────────
#    표본: 산업 ETF 29개마다 대표 티커 1개(SOXX→NVDA · IGV→MSFT · … · GDX→NEM). STOCK_UNIVERSE만 고치면 교체된다.
#    ★ v0.1.0의 자리매김: **배분·백테스트가 아니라 예측 채점**이 본체다. 라이브 규칙은 의도적으로 가장
#      단순한 것 하나(200일선 위/아래)로 고정하고, 펀더멘탈·어닝 후보 18종은 03 시트에서 소수 클래스
#      잣대로 먼저 측정한다. 이유: I 계층에서 '그럴듯한 근거로 라이브를 바꿨다가 세 라운드 연속 실패'했다.
#    ★★ 펀더멘탈 인과 처리(이 파일에서 가장 조심한 부분): 분기 재무는 **기간말이 아니라 유효일** 기준
#      as-of로만 쓴다 — (a) 실제 발표일 우선 (b) 없으면 기간말 + 60일(10-K 기한). '다음 어닝까지 며칠' 류는
#      과거 시점에 알 수 없으므로 **특성에 넣지 않았다**. 11_룩어헤드감사가 절단재계산으로 매 실행 검증한다.
#    시트: 00_실행요약 · 01Z_주식일별예측 · 01_일별_<티커>×29 · 02_티커요약 · 03_예측규칙정확도 ·
#      04_펀더멘탈원장(유효일·출처 포함) · 05_어닝이벤트(서프라이즈·PEAD 1/5/21일) · 06_성과요약 ·
#      11_룩어헤드감사 · 19_상승하락구간.
#    실행: run_pipeline.main(run_stock_layer=True) — 기본 True. 끄기: run_stock_layer=False.
#
#  ── 이번 실행에서 확인할 것(순서대로) ──────────────────────────────────────────────────
#    1) ★ **13f** — I★ 칼마 ≈ **3.620** · MDD ≈ **−0.1036** · CAGR ≈ **0.3749** · 한계비율 ≈ **2.000**
#       (리포트18 [확신캡격자] '강등 둘 다 없음' 행과 일치해야 한다 — 어긋나면 되돌림이 불완전하다).
#       ② MDD 악화 ≤ 0.010 **유지**되는지도 같이 본다.
#    2) ★ **19_상승하락구간 블록 A 29행** — 어느 산업이 '하락 방어 실패'이고 어느 산업이 '상승 미참여'인가.
#       그리고 ★ 전체 합계 행의 손실 출처 비율이 13l 블록 E의 '상승미달 63%'와 **같은 방향**인가(⑩(d)).
#    3) ★ **[확신캡격자] '경고강등만' 행**이 '강등 없음'을 칼마로 이기는가 — 이기면 W2만 다시 켠다(⑩(c)).
#    4) ★ **stock_regime_report.xlsx 03_예측규칙정확도 B블록** — F·E 계열(펀더멘탈·어닝) 중 연도비율 ≥ 0.60
#       & 정밀>기저 종목 ≥ 20/29 & 향후수익 격차 < 0 을 **동시에** 만족하는 규칙이 있는가(승격 후보).
#    5) **stock 11_룩어헤드감사 불일치 0건** · 02_티커요약의 '유효일 출처'가 '발표일'인 티커 수.
#    6) **리포트 3개(+K = 4개) 파일 크기 전부 30MB 이하** — 각 리포트 로그의 size_mb 확인.
#    7) [리더분산격자] 상위3(3.683) 재현 · 감사 11/11b·14 위반 0.
#    ⚠ 되돌리기 한 줄: i_overrides={"CAP_WARN_DOWNGRADE": True, "CAP_TREND_GUARD": True,
#      "CAP_WARN_WF_GATE": True} → v0.19.0과 비트 동일. 주식 계층 끄기: run_stock_layer=False.
#
#  VERSION: v1.15.0 - 2026-09-15 - [문서 + 실행 레시피 — 실행 로직 무변경] **I v0.18.0 → v0.19.0만** ·
#                    S v0.48.0 · M v1.53.1 무변경. REPORT58 구현분 — 산업(I)만.
#
#  사용자 지시(종전과 동일): "각 산업별 하락 예측 정확도를 더 올리면서 수익 곡선 상승시키도록 개선해
#  예측 틀린 부분이 왜 틀렸는지 뉴스 같은 것도 참고하면서 분석하고 개선해"
#
#  ★★ 리포트17 판정 — v0.18.0 사전등록 [검증] ⑧ **5조건 전부 PASS**(여섯 라운드 만의 첫 성공)
#    (a) 칼마 3.479 ≥ 3.328 ✔   (b) MDD −0.1113 → **−0.1068 개선** ✔
#    (c) 래더 3.479 > 무조건 동일캡 **2.771** ✔   (d) 달력 대조 3.419 / −0.1081 둘 다 이김 ✔
#    (e) 반증 행 3.463 < 라이브 3.479 ✔
#    ⑥ 한계비율 **0.7410 → 1.1330**(+53%). 평탄 CAP 50% 대비 CAGR·MDD·칼마 셋 다 개선.
#    지시 (나) 반영: 산업배분 합계 최대 **0.9500** · 0.9 이상 **88일** · 0.5 이상 **95일**(직전 0일).
#    13c 적용캡 CAP1.0 857일 / 강등 0.5 919일 / 보유 0.25 1,128일.
#
#  ★ "전체 산업 비중 1"은 측정 결과 **나쁘다** — [산업집중격자] 16행이 단조다
#    CAP축(폴백0%) 25% 3.620 · 50% 3.328 · 75% 3.021 · 100% 2.771 (CAGR은 역순 0.3647→0.3807)
#    폴백축 올릴수록 CAGR·칼마 둘 다 하락(25% 3.452 · 50% 3.275 · 100% 2.943)
#    [잔여격자] industries 노출 0.5674인데 CAGR 0.3381(S★ −2.06%p) · 칼마 2.788
#
#  ★ W3이 부수적으로 열어 준 것(INDUSTRY_GRID=False 동안 같이 생략돼 있었다)
#    **리더국면 NEUTRAL 칼마 3.787 · MDD −0.0955(= S★ 동일)** → 프로젝트 최초 S★ 칼마 3.758 돌파.
#    분산게이트 상위25% 3.767 · MDD −0.0955. ⚠ 둘 다 노출 0.0286 / 0.0138(거의 베팅 안 함) + 소격자 우승.
#
#  ── I v0.19.0 변경분(상세는 industry_rotation.py 헤더) ──────────────────────────────────
#    (X1) ★ **CAP_HOLD 0.25 → 0.0**(라이브) — 엔진이 '채택후보'로 표시한 행 승격.
#         확신1.0·보유0.0 = CAGR 0.3699 · MDD −0.1036 · 칼마 **3.571** · 한계비율 **1.383** ·
#         달력 대조(3.437 / −0.1071) 두 축 모두 승. 뜻: "확신이 유지되는 동안만 산업을 든다".
#    (X2) ★ **하락경고 워크포워드 산업 게이트**(라이브) — 강등을 적용연도 이전에 경고가 들었던 산업에만.
#         근거는 13p A4: JETS 정밀 0.9306(+0.449) ↔ **SOXX 0.0000(−0.386) · 경고일 향후수익 +11.364%**.
#         게이트 함수 오프라인 측정: 정밀−기저 0.048 → **0.084**, 같은 개수 알파벳 대조군은 **0.000 근처**.
#         ⚠ 연도 k/n은 6/8 → 4/7로 나빠진다(분모 상이 · 알파벳도 6/8이라 판별력 없음).
#         ⚠ 사후 SOXX 수정이 아니다 — 게이트는 2020~2023 SOXX를 **선택하고** 2024년부터 제외한다.
#    (X3) 신규 블록 **13p A5** — 위 측정을 엔진이 직접 낸다(게이트 3변형 + 알파벳 대조 + 인샘플 상한 + 연도별 목록).
#    (X4) 신규 격자 **[국면확신캡격자]** — 국면 3종(NEUTRAL / RISK_ON+NEUTRAL / 제약없음) × 확신캡 래더 + 달력 대조.
#    (X5) 판독문 정정 2건 — 13l F "하루 1~2개"는 실측 **최대 6개**(중앙값·평균 병기로 교체) ·
#         13l E 3분할 판독문을 보고서15(37건) → 현재 99건 숫자로 갱신.
#    ⚠ 측정하고 버린 설계: 저변동성 경고에 방향 조건(mom21<0 / mom63<0)을 붙이면 29산업 전수에서
#      정밀−기저 +0.081 → +0.033~0.046, 연도비율 0.627 → 0.41~0.45로 **전부 나빠진다**. 재시도 금지.
#
#  ── 이번 실행에서 확인할 것(순서대로) ──────────────────────────────────────────────────
#    1) ★ **13f** — I★ 칼마 ≥ **3.479** 이고 MDD가 **−0.1068보다 악화되지 않았는가**([검증] ⑨ (a)(b)).
#       기대: 칼마 ≈ 3.571 · MDD ≈ −0.1036 · CAGR ≈ 0.3699(리포트17 [확신캡격자] 행과 일치해야 한다).
#    2) ★ **13p 블록 A5** — WF 게이트 행이 (c) 알파벳 대조군을 정밀−기저로 이기고 (d) '① 전 산업' 행보다
#       정밀−기저·연도 k/n 둘 다에서 나은가. 연도별 선택 산업수(2018:0 · 2019:0 · 2020:8 …)도 확인.
#    3) ★ **[국면확신캡격자] 3행** — NEUTRAL 행이 자기 달력 대조군을 칼마·MDD 둘 다 이기는가([검증] ⑨ (e)).
#       그리고 래더와 겹쳐서 3.787을 넘는가, 아니면 두 장치가 같은 위험을 두 번 줄여 겹치지 않는가.
#    4) **13c 적용캡 열** — 값이 1.0 / 0.5 / **0.0** 세 가지로 나오는가(보유 tier가 0이 됐다).
#       00시트 총노출 행: 최대 0.9500 유지 · 0.9 이상 일수는 88 근처(강등 산업이 게이트로 줄면 늘어날 수 있다).
#    5) **[확신캡격자] 되돌림 후보 행**(확신1.0·보유0.25)이 리포트17의 3.479를 재현하는가 — 재현되면
#       비교 기준선이 살아 있다는 뜻이고, (a)(b)가 걸릴 때 되돌릴 목표가 된다.
#    6) **13l 블록 E/F** — E 3분할이 여전히 단조인가(약세 > 중간 > 강세). F 판독문이 정정됐는가.
#    7) [리더분산격자] 상위3(3.683) **세 번째 재현** 여부 · [리더추세게이트격자] 2행 재현.
#    8) **감사·정합** 11(0/174)·11b(0/2262)·14 위반 0.
#    ⚠ 되돌리기 한 줄: i_overrides={"CAP_HOLD": 0.25, "CAP_WARN_WF_GATE": False} → v0.18.0과 비트 동일.
#
#  VERSION: v1.14.0 - 2026-09-15 - [문서 + 실행 레시피 — 실행 로직 무변경] **I v0.17.0 → v0.18.0만** ·
#                    S v0.48.0 · M v1.53.1 무변경. REPORT57 구현분 — 산업(I)만.
#
#  사용자 지시 두 갈래:
#    (가) "각 산업별 하락 예측 정확도를 더 올리면서 수익 곡선 상승시키도록 개선해 예측 틀린 부분이
#         왜 틀렸는지 뉴스 같은 것도 참고하면서 분석하고 개선해"
#    (나) ★ "그리고 보니까 전체 산업 비중 1로 하고 상승이 확실하면 비중을 최대 1까지 늘려야지
#         왜 전체로 한 날이 없어 개선해"
#
#  ★ 보고서 16 판정 요약
#    ① T1 **PASS** — 13j '하락 브레이크' 청산 **0건**(보고서15의 허위 라벨 8건 소멸).
#    ② T3 **PASS** — 13l 블록 E 산출: 상승미달 34건 −134.41%p · 하락증폭 21건 −79.84%p ·
#       방어성공 22건 +52.47 · 랠리초과 46건 +246.03 → **상승미달이 손실의 63%**(13j의 62% 재현).
#    ③ T4 **작동** — 01Z '하락경고 산업수(A3-3)' 평균 1.04 · 최대 23, 날마다 값이 다르다(배선 정상).
#    ④ T2 [리더추세게이트격자] 3행 중 **2행 PASS**(리더끔 3.395 vs 3.160 · K=3분산 3.422 vs 3.240),
#       몫절반은 MDD로 FAIL(−0.1097 vs −0.1090).
#    ⑤ **[리더분산격자] 상위3이 여전히 최고 다리 구성 3.683**(리포트15·16 두 번 재현) — 추세게이트 전 행보다 높다.
#       ★ K=1 3.328 · 상위2 3.631 · 알파벳 대조 3.367/3.368 · S★ 3.758.
#    ⑥ ⑥ 한계비율 **0.7410** — 다섯 라운드째 같은 산술이다(0.74 < S★ 칼마 3.756).
#
#  ★ (나)의 원인 확정 — 13c 반사실 재구성(오차 0.0)
#    산업배분 합계: 평균 0.0804 · **중위 0.0000** · 최대 0.4778 · **0.5 이상 0일** · 정확히 0인 날 1,414일(64.7%).
#    곱해진 세 원인이며 축이 다르다:
#      (1) INDUSTRY_LEADER_CAP 0.5      → 천장이 절반
#      (2) INDUSTRY_FALLBACK_SHARE 0.0  → 리더 없는 날 0 = **중위 0의 유일한 원인**
#      (3) 리더 성립 13.3% · 하루 리더 부모 1~2개 → 최대도 0.48에서 막힘
#    확신 게이트 통과일 CAP 1.0 반사실 → 최대 **0.9500** · 0.9 이상 **146일** · 0.5 이상 212일.
#
#  ── I v0.18.0 변경분(요약 — 상세는 industry_rotation.py 헤더) ────────────────────────────
#    (W1) ★ **확신 등급별 리더 캡** — `_mk_target_w(cap_mat=)` 신설. 확신 게이트 통과 **1.0** /
#         하락경고 강등 **0.5** / 보유 중 확신 미달 **0.25**. ⚠ 비중(위험) 파라미터 변경.
#         근거: 엔진 13l 블록 B 실측 확신 통과 +0.2897%/21일 vs 미달 +0.0597%(4.85배).
#         평탄 CAP 1.0(순수 레버업)을 쓰지 않은 이유: 한계비율 0.74 < 칼마 3.756이라 **산술적으로 진다**.
#    (W2) 하락경고(A3-3 정의)를 **청산이 아니라 확신 강등**으로 사용. v0.15.0 브레이크(몫을 0으로)의
#         실패 양식을 반복하지 않도록 '상향 보류'로만 쓴다.
#    (W3) ★ **INDUSTRY_GRID False → True** — [산업집중격자] CAP 4종 × 폴백 4종 재개방. v0.10.0 H3가
#         끈 사유("리더 0일")는 v0.14.0이 리더일을 0 → 2,903으로 되살려 소멸했다.
#    (W4) 신규 **[확신캡격자]** 4행 + 대조군 2종/행(무조건 동일캡 = 순수 레버업 · 달력 = 같은 날수 무신호).
#    (W5) 신규 **13l 블록 F 총노출 천장 분해** + 00_실행요약 2행 + 13c **'리더 적용캡'** 열.
#    (W6) 신규 **13p 블록 A4 하락경고 산업별 정확도**(정밀도 vs 기저 · 연도 k/n · 향후수익 격차).
#
#  ── 이번 실행에서 확인할 것(순서대로) ──────────────────────────────────────────────────
#    1) **00_실행요약 '★ 산업 총노출' 2행**을 먼저 본다. '0.9이상' 일수가 0이 아니면 (나) 지시가 반영됐다.
#       13c 'XLK 리더 적용캡' 등 열에 1.0 / 0.5 / 0.25 세 값이 모두 나타나는지도 함께 본다(래더 작동 증거).
#    2) ★ **사전등록 되돌림 판정**(I 헤더 [검증] ⑧) — 13f에서 I★ 칼마가 **3.328 이상**인가,
#       MDD가 v0.17.0 ★ 대비 **0.5%p 이상 악화**되지 않았는가. 하나라도 걸리면 v0.19.0에서 되돌린다.
#    3) ★ **[확신캡격자]** — 각 래더 행이 `대조: … 무조건 동일캡`과 `대조: … 같은날수 달력` **둘 다**를
#       칼마·MDD로 이겼는가. 무조건 대조에 지면 확신 선택에 값이 없다는 뜻이다(설계 폐기).
#    4) ★ **[산업집중격자] 16행** — 리더가 작동하는 상태의 첫 측정이다. 특히 **폴백 25/50/100%** 행이
#       중위 노출을 올리면서 칼마를 지키는가. (CAP 1.0·폴백 0%)와 (CAP 0.5·폴백 0%)의 차이가 W1의 상한이다.
#    5) **13p 블록 A4** — 경고가 듣는 산업이 몇 개인가(정밀−기저 > 0 & 연도비율 ≥ 0.6). 산업별 취사선택 근거.
#    6) **13l 블록 F** — (1)(2)(3) 세 줄이 실제 숫자로 나오는지. (2)의 '0.9이상' 일수가 CAP 상한을 알려 준다.
#    7) [리더분산격자] 상위3(3.683)·[리더추세게이트격자] 2행이 **다시 재현**되는지 — 세 번째 재현이면
#       다음 라운드에 라이브 후보다.
#    8) **시트 전수** 13·13b·13c·13c2·13f·13j·13l·14·15·16·17·18 + 격자에 **[산업집중격자]·[확신캡격자]**·
#       [리더분산격자]·[하락경고격자]·[리더위험격자]·[리더추세게이트격자] 등장.
#    ⚠ 되돌리기 한 줄: i_overrides={"INDUSTRY_CAP_BY_CONVICTION": False, "INDUSTRY_GRID": False}
#      → v0.17.0과 비트 동일(래더 끔 + 재개방 격자 끔).
#
#  VERSION: v1.13.0 - 2026-09-15 - [문서 + 실행 레시피 — 실행 로직 무변경] **I v0.16.0 → v0.17.0만** ·
#                    S v0.48.0 · M v1.53.1 무변경. REPORT56 구현분 — 산업(I)만.
#
#  사용자 지시: "각 산업별 하락 예측 정확도를 더 올리면서 수익 곡선 상승시키도록 개선해 예측 틀린 부분이
#  왜 틀렸는지 뉴스 같은 것도 참고하면서 분석하고 개선해"
#
#  ★ 보고서 15 판정 — 되돌림 완전 · [리더분산격자] 통과 · [하락경고격자] 실패
#    ① 되돌림 **PASS**: 13f가 보고서13과 비트 동일(CAGR 0.3704 · MDD −0.1113 · 칼마 3.328 · ① PASS),
#       18 병리 6개 복귀, ⑥ 한계비율 **0.7410**(v0.16.0 예측 0.74와 일치).
#    ② [리더분산격자] **PASS — 세 기준 전부**:
#         구성   CAGR    이득      MDD      악화     칼마    알파벳대조 칼마
#         K=1   0.3704  +1.17%p  −0.1113  +1.58%p  3.328   —
#         K=2   0.3567  −0.20%p  −0.0982  +0.28%p  3.631   3.368
#         K=3   0.3553  −0.34%p  −0.0965  **+0.10%p**  **3.683**   3.367
#         S★    0.3587  —        −0.0955  —        3.758   —
#       (a) 칼마 > 3.328 ✓ (b) 알파벳 대조군을 칼마·MDD 둘 다 이김 ✓ (c) MDD 악화 1.58 → **0.10%p** ✓
#       2024년 MDD는 −0.1113 → **−0.0816**으로 S★(−0.0850)보다도 좋아졌다.
#       ⚠ 그래도 칼마가 S★ 미달(3.683 < 3.758)이고 이유가 드러났다: **분산이 수익을 죽인다**
#       (이득 +1.17 → −0.20 → −0.34%p). **수익 전체가 top-1 한 칸**에 있다(편포 · 왜도 +0.746).
#    ⑤ [하락경고격자] **FAIL**: 3행 중 2행이 달력 대조군에 졌다(3.291 vs 3.338 · 3.310 vs 3.339).
#       경고의 예측력은 진짜인데 리더에 적용하면 무작위 날짜 제거보다 못하다 — 격자로 돌린 판단이 맞았다.
#    ⑦ A3 불변 **PASS**(보고서14와 동일: A3-3 정밀도 0.5695 · 기저 0.4263 · 연도비율 0.688).
#
#  ★ 새 진단 — **손실의 62%는 '하락'이 아니라 '상승 미달'이다**
#    리더 거래 52건 4분류:  ① 상승미달 **16건 −44.89%p** · ② 하락증폭 8건 −27.40%p ·
#      ③ 방어성공 6건 · ④ 랠리초과 22건(기여 **+56.91%p**).  미달 24건 중 16건이 ①이다.
#    원인: 채택 신호-연도의 2/3이 사전방향 −1(SCORE_PCT 9년 · SCORE_MINUS_PARENT 9년) —
#      **부모 안에서 점수가 가장 낮은 산업을 고르는 역추세 베팅**이다. 그래서 '리더>부모' 0.4711(동전던지기
#      미달)인데 평균 초과는 플러스다. ⚠ 부호 문제가 아니다(13g 사전방향 근거에 이미 평균회귀로 등록됨).
#    **언제 깨지는가 — 부모가 이미 강세일 때**(진입 시점 정보만으로 3분할):
#      부모 63일 모멘텀  약세 13건 **+5.04%p**·상승미달 **0.077**·기여 **+39.9**
#                      중간 12건 −0.68·0.250·+4.2
#                      강세 12건 +0.32·**0.417**·+14.7
#      ext200으로도 단조(약세 +3.91 / 중간 +1.62 / 강세 **−0.75**), m21·m252·dd252 모두 같은 방향,
#      연도 편중도 아니다(약세 버킷에 2019·2020·2023·2024·2025·2026 분산).
#    뉴스 대조: **2026-09-10**(데이터 마감 9/11 직전) 구리가 4거래일 연속 신고가 직후 하루 −5.4%,
#      은 −5.9%, 백금·팔라듐 −6%대, Freeport·Teck −6~7%. 정제구리 관세 미결정(Reuters) + 유가 $105·
#      9월 연준 인상 확률 ~70%. **"가격 추세에 사전 경고가 없었다."** XME·GDX가 이 시스템에 있고
#      이 사건은 표본 안이다. 2026-07 SOX −21% · 2026-02 AI 대체 내러티브 · 2024-08 엔 캐리와 같은 성질이다.
#    → 예측해서 피하는 것이 아니라 **강세 구간에서 역추세 집중을 하지 않는 것**이 현실적 대응이다(T2).
#
#  ⚠ 그리고 산업별 하락 예측이 **라벨 차원에서는 존재하지 않는다**
#    00시트 '다음 거래일 예측'이 **29산업 전부 "상승(위험선호) / 목표비중 1.00 / 추가매수"**로 똑같다
#    (INDUSTRY_REGIME_SOURCE="m_inherit"이라 산업별 라벨 차이 0 — v0.14.0 C3의 귀결).
#    A3 경고는 산업별로 다른데 01_일별에만 있었다. **T4가 00·01Z에 올린다**(신호 무변경 · 표시만).
#
#  ⚠ 기본값 변경 — **없다**. 라이브 신호·위험 파라미터 변경 0건(버그 수정 + 진단 추가만).
#    (T1 버그) 13j 청산 사유의 '하락 브레이크'가 **INDUSTRY_LEADER_BRAKE를 확인하지 않았다** —
#      v0.16.0에서 껐는데도 보고서 15에 **8건** 찍혔다(배분엔 영향 없음 · 13f 비트 동일이 증거).
#      청산 원인을 잘못 적으면 다음 라운드가 '브레이크가 일했다'고 오독한다. config 게이트 추가.
#    (T2 신규 격자) **[리더추세게이트격자]** — 부모 ext200 자기이력 상위 1/3(강세)일 때
#      (a) 리더 끔 (b) K=3 분산 (c) 몫 절반 + 각 행에 **달력 대조군**. ⚠ 표본 37건이라 라이브 아님.
#      되돌리기: i_overrides={"INDUSTRY_TREND_GATE_GRID": ()}
#    (T3 진단) 13j에 '결과 유형' 열, **13l 블록 E '실패 유형'**(유형별 집계 + ★손실 분해 + 부모 추세 3분할).
#    (T4 진단) 00시트 산업 줄에 **⚠ 하락경고(A3-3)** 표시 + 경고 요약 행, 01Z에 '하락경고 산업수(A3-3)' 열.
#      되돌리기: i_overrides={"SHOW_DECLINE_WARN_SUMMARY": False}
#
#  실행은 한 번이면 된다(S·M 무변경):  out = RP.main()
#
#  ★ 이번 실행에서 볼 것 — 사전 고정:
#    1) **T1 확인(가장 먼저).** 13j_배분거래내역 청산 사유에 '하락 브레이크'가 **0건**이어야 한다
#       (브레이크 OFF 상태이므로). 그리고 13f·18_리더위험진단·13l A/B·13p는 보고서15와 **동일**해야 한다
#       — T1은 라벨만 고쳤으므로 숫자가 변하면 다른 것이 섞인 것이다.
#    2) **T3 블록 E(새 진단).** 13l에 'E. 실패 유형'이 나오고 보고서15의 13j 실측을 재현해야 한다:
#       ① 상승미달 · ② 하락증폭 · ③ 방어성공 · ④ 랠리초과 4행 + **★ 손실 분해** 행에서
#       상승미달이 손실의 **60% 내외**로 찍히는지. 그리고 '진입시 부모 추세 3분할'에서
#       **약세 구간의 평균초과가 강세 구간보다 크고, 상승미달 비율은 강세가 더 높아야** 한다
#       (보고서15 기준 약세 +5.04%p·0.077 / 강세 +0.32%p·0.417).
#       ⚠ 13l 에피소드(98회) 기준과 13j 거래(52건) 기준은 집계 단위가 달라 수치가 정확히 같지 않다 —
#         **방향과 순서**가 같은지를 본다.
#    3) **T2 [리더추세게이트격자] 판정(본 시험).** 13_산업배분전략에서 `추세게이트 …` 3행이
#       · (a) 칼마 > **3.328**(★ K=1) 그리고
#       · (b) 같은 행의 `대조: … 같은날수 달력(신호없음)`보다 **칼마·MDD 둘 다** 높음
#       둘을 다 만족한 행만 후보. ③ 그 행에서 13l 블록 E의 **상승미달 건수가 줄어야** 한다
#       (그것이 T2가 의도대로 작동했다는 직접 증거다).
#       ⚠ (b)를 못 이기면 '강세 회피'가 신호가 아니라 노출 축소 효과였다는 뜻이다 — 표본 37건이니 충분히 가능하다.
#    4) **T4 확인.** 00_실행요약 산업 줄에 '⚠ 하락경고(A3-3)' 또는 '경고 없음'이 붙고,
#       '⚠ 산업별 하락 경고 요약' 행에 경고 산업 수가 나온다. **경고가 29개 전부이면 배선 오류**다
#       (산업별로 달라야 의미가 있다). 01Z에 '하락경고 산업수(A3-3)' 열이 생기고 날마다 값이 변해야 한다
#       — '하락예측 산업수'가 0 또는 29로 고정된 날에도 이 열은 변해야 한다(그게 T4의 요점이다).
#    5) **13p 블록 A3 불변** — A3-3 0.5695/0.4263/0.688/−2.403/−1.05 · A3-4 0.5263/0.677/−2.004/−2.707.
#    6) **[리더분산격자] 재확인** — K=2 3.631 · K=3 3.683 · 알파벳 대조 3.368/3.367이 재현되는지.
#       재현되면 다음 라운드에 K를 라이브로 올릴지 판단한다(단 CAGR 이득이 음수라는 점을 함께 봐야 한다).
#    7) **시트 전수** 13·13b·13c·13c2·13f·13j·13l·14·15·16·17·18 + 격자에 [리더분산격자]·[하락경고격자]·
#       [리더위험격자]·**[리더추세게이트격자]** 등장.
#    8) **감사 0건** · **14_계층정합 위반 0**.
#    9) **S·M 무변경** — S★ 35.87/2.213/−9.55 · M★ 25.50/2.236/−7.07.
#
#  [검증] 이 세션에는 시장데이터·정식 회귀 스위트가 없어 위 9항목을 실제로 실행하지 못했다. 한 것:
#    (1) ast.parse·import 성공, IndustryConfig 기본값 확인(브레이크 OFF · TOPK 1 · 격자 4종)
#    (2) 합성데이터 단위테스트 5종 **224 PASS / 0 FAIL**
#        (test_i017 62 · test_i016 43 · test_i015 52 · test_i014 43 · test_i014_sheet17 24):
#        · T1 게이트가 brake 시계열 접근보다 **먼저** 오는지 소스 순서로 확인 + 3가지 조합 재현
#          (ON+제동 → 라벨 / **OFF+제동 → 라벨 없음**(8건 버그) / ON+비제동 → 없음)
#        · T2 강세 판정: 설계 구간 분리 · 이력 부족 구간 무판정 · **절단재계산 최대차 0(룩어헤드 없음)** ·
#          sres['bh_ret'] 경로 사용 · 자기이력 expanding 백분위 사용 · 달력 대조군 동반
#        · T3 4유형 분류 경계값 6종(부모 0%·동률 포함) + 13l 블록 E·손실 분해·추세 3분할·13j 열
#        · T4 정의가 A3-3과 일치 · 00 경고 표시·요약 행 · 왜 필요한지 설명 · 진단 전용 명시 ·
#          01Z 열 추가 및 '산업배분 합계' 인덱스 6 → 7 보정
#        · 라이브 기본값 불변(브레이크 OFF · TOPK 1 · ADOPT_BY order · CAP 0.5 · SLEEVE 0.0 · m_inherit)
#        · 이전 라운드 산출물 9종 + R5 한계비율 행 보존
#    ⚠ **라이브 신호·위험 파라미터 변경 0건**이다. 세 라운드 연속으로 '그럴듯한 장치'가 실패했으므로
#      (v0.15.0 브레이크 · v0.14.0 판정 기준 착오 · v0.16.0 경고 격자 FAIL) 새 아이디어는 전부
#      격자 + 대조군으로만 검정한다. 이번 라운드의 사용자 가치는 (가) 버그 수정 (나) 손실의 진짜 원인을
#      보이게 함(블록 E) (다) 산업별 하락 경고를 요약에 노출(T4)이다.
#    **로컬에서 반드시 필요한 것**: 위 9항목 실측, 회귀 스위트 재실행 + 기본값 가드 갱신,
#    그리고 회귀 테스트 3종 신설 — (1) 13l 리더일 > 0 (2) 13p 블록 B 순서 k/n (3) **13l 블록 E 상승미달 건수**.
#    (3)이 없으면 T2의 효과를 다음 라운드에 확인할 수 없다.
#
#  ---- v1.12.0 원문(참고) ----
#  VERSION: v1.12.0 - 2026-09-14 - [문서 + 실행 레시피 — 실행 로직 무변경] **I v0.15.0 → v0.16.0만** ·
#                    S v0.48.0 · M v1.53.1 무변경. REPORT55 구현분 — 산업(I)만.
#
#  사용자 지시: "결과인데 수익곡선이 더 안좋아졌잖아 각 산업별 하락 예측 정확도를 더 올리면서 수익 곡선
#  상승시키도록 개선해 예측 틀린 부분이 왜 틀렸는지 뉴스 같은 것도 참고하면서 분석하고 개선해"
#  → **사용자 지적이 맞다.** v0.15.0 R1(리더 하락 브레이크)의 사전등록 3기준이 전부 실패했고 되돌린다.
#
#  ★ 보고서 14 판정 — R1 전면 실패
#    항목            보고서13(v0.14.0)  보고서14(v0.15.0)  사전등록          판정
#    CAGR            0.3704            0.3606            ≥ 0.3654        **FAIL**
#    MDD             −0.1113           −0.1060           악화 ≤ 0.010 → 0.0105  **FAIL**
#    칼마             3.328             3.403             > S★ 3.758      **FAIL**
#    13f ① CAGR      PASS              **FAIL**(0.0019)  —               퇴행
#    18 병리          6개                4개               ≤ 2개           **FAIL**
#    13j 브레이크 청산  0건                24건              ≥ 5건           PASS
#
#  ★ 왜 실패했나 — 2024년 한 에피소드 과적합
#    연도별 I★−S★(%p): 2020 **−3.46→−7.16** · 2024 +3.83→+4.62 · 2025 **+8.76→+2.59** · 2026 +4.06→+2.59
#    브레이크 청산 24건을 낙폭으로 갈라 보면 dd63 ≤ −10%로 끊은 9건의 전략기여 **−2.740%p**,
#    dd63 > −10%로 끊은 15건은 +16.079%p. 즉 **깊은 낙폭+고변동성은 바닥 근처이고 다음 21일 수익이 플러스**다.
#    ⚠ v0.15.0 헤더에 이미 "vol21 상위20%는 향후 21일 하락을 못 맞춘다(연도비율 0.396)"고 적어 두었는데,
#    "수익률이 아니라 correctness"라는 프레임으로 그 함의를 무시하고 반대 방향 장치를 만든 것이 착오였다.
#    [리더위험격자]는 제 일을 했다 — 3행 전부 달력 대조군을 이겼지만(신호는 무작위보다 낫다) 전부 S★ 칼마 미달이고
#    dd 조건은 MDD를 악화시켰다(dd10만 −0.1138). '신호가 낫다'와 '전략이 좋아진다'는 별개다.
#
#  ⚠ 지난 라운드 보고 오류 정정
#    v1.11.0/v0.15.0 문서의 "부모안 적중 0.378 vs 무작위 0.343 — 8/8년 초과"는 **엔진이 아니라 작성자
#    시뮬레이션** 값이었다. 엔진 13p 블록 B(보고서 13·14 동일):
#      연도      2018  2019  2020  2021  2022  2023  2024  2025  2026
#      리더→1위   0.228 0.370 0.426 0.227 0.059 0.047 0.253 0.510 0.419
#      무작위     0.258 0.353 0.317 0.277 0.306 0.279 0.306 0.330 0.319
#    → **무작위 초과 4/9년**(v0.14.0 문턱은 7/9였다) · **리더>부모 ETF 평균 0.4711**(동전던지기 미달).
#    리더의 평균 초과는 순서가 아니라 **편포**에서 나온다(왜도 +0.746 · 첨도 20.43 · IR 0.240).
#    v0.16.0 R2가 이 정정을 00시트 헤드라인으로 올린다.
#
#  ★ 칼마가 오르지 않는 산술(R5로 매 실행 표시)
#    한계비율 = (I★ CAGR − S★ CAGR) / (I★ MDD 악화) = 1.17/1.58 = **0.74** vs S★ 칼마 **3.756**.
#    한계비율이 기존 칼마보다 작은 다리를 더하면 칼마는 **반드시** 내려간다. 노출 축소(CAP 0.25 → 3.584)나
#    타이밍(브레이크 → 3.403~3.419)으로는 못 넘는다. 넘는 길은 (가) 정보비율 올리기(현재 0.240) 또는
#    (나) **개별 산업 위험 줄이기** — (나)가 이번 격자다.
#
#  ★ 하락 예측은 실제로 올랐다(이번 라운드 유일한 성공 · 엔진 13p 블록 A3 실측)
#    규칙                      정밀도  기저    정밀−기저 연도비율 격차(%p) 2024격차 MCC>0
#    ⑥ 확정 상승아님(현행)       0.4762 0.4263 +0.050  0.471  −0.84   +0.226  27/29
#    **A3-3 ⑥&vol33&탈동조      0.5695 0.4263 +0.143  0.688  −2.403  −1.05**  24/29
#    **A3-4 vol21 하위20%       0.5263 0.4263 +0.100  0.677  −2.004  −2.707** 25/29
#    다섯 라운드 중 가장 강한 예측 결과이고 지난 라운드 걸림돌이던 **2024년 격차가 음수로 돌아섰다**.
#    ⚠ 그런데 리더에 적용할 근거는 아직 얇다: 경고&리더보유 255/934일(27.5%)로 겹치지만 경고&보유 ≥20일인
#      산업 7개 중 격차 음수는 IGV(−7.38%p)·IYZ뿐이고 SOXX(+1.71)·IHE(+1.88)·IHF(+2.98)는 양수다 —
#      총계가 IGV 한 종목에 끌린다. R1과 같은 함정이므로 **라이브가 아니라 격자로** 검정한다.
#
#  ⚠ 기본값 변경 1건 — **되돌림뿐이다**:
#    **R1a — I.INDUSTRY_LEADER_BRAKE True → False.** 사전등록 규칙 집행.
#      CAGR 0.3606 → 0.3704 복원(13f ① 다시 PASS), MDD −0.1060 → −0.1113로 되돌아간다.
#      브레이크 코드·18시트·[리더위험격자]는 **지우지 않았다**(재검정 가능성 보존 — 프로젝트 규약).
#      ⚠ 다시 켜기: i_overrides={"INDUSTRY_LEADER_BRAKE": True}
#
#  신규 격자·진단 4건(라이브 신호 변경 없음):
#    (R2) 00시트에 **엔진 기준 순서 판정** 헤드라인 + "8/8년" 주장 정정.
#    (R3) **[리더분산격자]** ROTATION_LEADER_TOPK — 리더 몫을 부모 안 복합순위 **상위 K개**에 균등 분할
#      (K=1이 라이브·비트 동일). 총 노출 불변, 개별 산업 위험만 줄인다. 격자 (2,3) + 각 K마다
#      **알파벳 순 대조군**(순위 대신 알파벳으로 K개) — '순위가 값을 하는가' vs '그냥 분산 덕인가'를 가른다.
#    (R4) **[하락경고격자]** A3-3/A3-4 경고일에 리더 몫 전량/절반 감축 + **달력 대조군**(같은 날수·신호 없음).
#    (R5) **13f에 ⑥ 한계비율 행** — 이 값이 S★ 칼마보다 작으면 칼마는 구조적으로 오를 수 없다는 산술을 상시 표시.
#
#  실행은 한 번이면 된다(S·M 무변경):  out = RP.main()
#
#  ★ 이번 실행에서 볼 것 — 사전 고정:
#    1) **R1a 되돌림이 완전한가(필수 · 가장 먼저).** 13f에서
#       · ① CAGR 기준이 **PASS로 복귀**(I★ CAGR ≈ 0.3704, 실측 ≈ 0.0117)
#       · I★ MDD ≈ **−0.1113** · 칼마 ≈ **3.328**
#       즉 **보고서 13과 일치**해야 한다. 다르면 되돌림이 불완전한 것이니 그것부터 잡을 것.
#       (13j에 '하락 브레이크' 청산이 0건이어야 하고, 18_리더위험진단 병리가 다시 6개여야 한다.)
#    2) **R3 [리더분산격자] 판정(이번 라운드의 본 시험).** 13_산업배분전략에서 `리더분산 상위2/상위3`이
#       · (a) 칼마 > **3.328**(K=1 = ★) 그리고
#       · (b) 같은 K의 `대조: 상위K 알파벳순(순위 미사용)` 행보다 칼마·MDD 둘 다 높음
#       둘을 **다 만족하는 행만** 다음 라운드 채택 후보다. (b)를 못 이기면 '순위가 아니라 분산 덕'이므로
#       리더 선택 로직 자체를 재설계해야 한다는 뜻이다(블록 B 4/9년과 일관된다).
#       · (c) 그 행의 MDD 악화가 K=1보다 **작아야** 한다 — 분산의 목적이 그것이므로.
#    3) **R5 한계비율(⑥ 행).** 13f 마지막 행에서 K=1의 한계비율이 0.74 근처인지, 그리고 [리더분산격자]
#       행들의 CAGR/MDD가 한계비율을 S★ 칼마(3.758) 쪽으로 밀어 올렸는지 본다. 이 산술을 못 넘으면
#       어떤 타이밍 장치도 칼마를 못 올린다 — 두 라운드가 그것을 증명했다.
#    4) **R4 [하락경고격자].** 각 `하락경고 …` 행이 같은 이름의 `대조: … 같은날수 달력(신호없음)`을
#       **칼마·MDD 둘 다** 이겨야 후보. ⚠ 이기더라도 라이브 승격은 다음 라운드 판단이다(IGV 편중 우려).
#    5) **R2 순서 판정 헤드라인.** 00_실행요약에 '리더 → 부모 안 실현 1위 > 무작위 인 연도 4/9'와
#       '리더 > 부모 ETF 평균 0.4711'이 찍히는지, 그리고 정정 문구가 들어갔는지 확인.
#       ⚠ 사용자 잣대(순환매는 순서로 판정)로는 이 4/9가 **미달**이다 — 13f ①이 PASS여도
#       순환매로서 검증된 것은 아니라는 점을 같은 자리에서 읽히게 했다.
#    6) **13p 블록 A3 유지.** A3-3/A3-4의 연도비율·격차·2024격차가 보고서14와 같아야 한다
#       (A3-3: 0.688 / −2.403 / −1.05 · A3-4: 0.677 / −2.004 / −2.707). 브레이크를 껐어도 A3는
#       리더와 무관한 산업 단위 진단이므로 **변하지 않아야 한다** — 변하면 배선이 잘못된 것이다.
#    7) **시트 전수** 13·13b·13c·13c2·13f·13j·13l·14·15·16·17·18 + 격자 행에 [리더분산격자]·[하락경고격자] 등장.
#    8) **감사 0건** · **14_계층정합 위반 0**(분산은 총 노출을 바꾸지 않으므로 필수 확인).
#    9) **S·M 무변경** — S★ 35.87/2.213/−9.55 · M★ 25.50/2.236/−7.07.
#
#  [검증] 이 세션에는 시장데이터·정식 회귀 스위트가 없어 위 9항목을 실제로 실행하지 못했다. 한 것:
#    (1) ast.parse·import 성공, IndustryConfig 기본값 확인(BRAKE=False · TOPK=1 · 격자 2종 등록)
#    (2) 합성데이터 단위테스트 4종 **162 PASS / 0 FAIL**
#        (test_i016 43 · test_i015 52 · test_i014 43 · test_i014_sheet17 24):
#        · R1a 되돌림 확인 + 브레이크 스위치·코드·격자가 살아 있음(재검정 가능) + 다시 켜기 동작
#        · R3 top-K 분배: **합 = 1.0(총 노출 불변)** · 균등 분할 · **부적격 산업 배제**(순위 1등이어도) ·
#          K > 적격수면 적격 전체로 제한 · NaN 순위는 후순위 · **알파벳 대조군이 순위와 다른 선택을 한다**
#        · R4 격자 형식(라벨/분위/탈동조/감축비율) · 절반 감축 행 존재 · 되돌리기
#        · R5 한계비율 산술 0.7405 = 1.17/1.58 이 S★ 칼마 3.756 미달임을 확인(보고서14도 0.181로 동일 결론)
#        · 앞선 라운드 산출물(브레이크·18시트·A3·결합국면·17시트·연도판정 헬퍼) 전부 보존
#    (3) v0.15.0 테스트를 '라이브 기본값 단정' → '기능 회귀 가드'로 고쳤다(브레이크를 켜서 기능만 검증).
#    ⚠ 이번 라운드는 **라이브 변경이 되돌림 1건뿐**이다. 두 라운드 연속으로 그럴듯한 장치를 라이브에 넣어
#      실패했으므로(R1 브레이크 · 그 전 라운드의 판정 기준 착오) 새 아이디어는 전부 격자 + 대조군으로만 검정한다.
#    **로컬에서 반드시 필요한 것**: 위 9항목 실측, 회귀 스위트 재실행 + 기본값 가드 갱신
#    (INDUSTRY_LEADER_BRAKE=False · ROTATION_LEADER_TOPK=1 · ROTATION_ADOPT_BY="order" · COUPLING_STATE=True),
#    그리고 **13l 리더일 > 0 회귀 테스트**와 **13p 블록 B 순서 판정 k/n 회귀 테스트**(4/9가 조용히 더
#    나빠지는 것을 잡기 위해 — 이번 라운드에 그것을 놓쳤던 것이 문제였다).
#
#  ---- v1.11.0 원문(참고) ----
#  VERSION: v1.11.0 - 2026-09-14 - [문서 + 실행 레시피 — 실행 로직 무변경] **I v0.14.0 → v0.15.0만** ·
#                    S v0.48.0 · M v1.53.1 무변경. REPORT54 구현분 — 산업(I)만.
#
#  사용자 지시: "이제야 좀 의미있어진 것 같은데 아직 멀었어 각 산업별 하락 예측 정확도를 더 올리면서
#  수익 곡선 상승시키도록 개선해 예측 틀린 부분이 왜 틀렸는지 뉴스 같은 것도 참고하면서 분석하고 개선해"
#
#  ★ 보고서 13 판정: v0.14.0의 P1은 성공했고, 하락 예측은 여전히 실패했다.
#    성공 — 13l 리더일 **0 → 2,903일** · 에피소드 124회(승률 0.553) · h=21 리더초과 +0.517% vs 타산업 +0.170%
#      · h=63 +1.203% vs −0.253% · **I★ CAGR 0.3704 vs S★ 0.3587 = +1.17%p로 13f ① 처음 PASS**
#      · 17 블록 A 단조(강결합 0.1501 → 보통 0.1931 → 탈동조 0.2355) · 블록 C 부모베타 ⚠ 반전 확인
#      · 14 위반 0 · 11·11b 감사 이상 없음
#    실패 — **MDD −0.1113 vs S★ −0.0955**(② FAIL) · **칼마 3.328 < 3.758**(⑤ FAIL) · MDD 발생일 2024-08-07
#      · 하락 예측 ⑥의 연도×산업 정밀도>기저 **121/261 = 46.4%**(v0.14.0 P4 합격선 0.60 미달)
#
#  ★ 왜 틀렸나(거래 52건 + 에피소드 98개 전수 + 뉴스 대조):
#    (D1 ★) **낙폭이 깊어지는 동안 비중이 늘어난다.** 낙폭 2%p 이상 악화된 24개 에피소드에서
#      복합점수백분위가 **오른** 경우 16개(66.7%) · 위험점수 H가 **내린** 경우 17개(70.8%).
#      최악 SOXX 2024-06-25~08-16(38일): 비중 0.180 → **0.450** 동안 낙폭 −0.1610(최저 −0.2493),
#      점수 0.653 → 0.930, H 0.128 → 0.053. **백테스트 성적이 아니라 그 자체로 틀린 동작이다.**
#    (D2) 13j 청산 사유 전수에 **하락 때문에 나온 건이 0건** — 하락 청산 장치가 아예 없었다.
#    (D3) 보유 11~21일 구간이 최악(19건 · 벤치대비 −1.335%p · 승률 0.263).
#    (D4) 기여가 XLK에 쏠림(52건 중 24건 · 57.59%p / 총 58.84%p). XLI·XLC·XLV·XLF는 순마이너스.
#    (D5) 산업 다리: 연율 +0.642%p · 변동성 2.674%p · **정보비율 0.240** · 누적초과 낙폭 −7.83%p @ 2024-08-07.
#      고베타 베팅은 아니다(리더일 가중 부모대비베타 1.036 vs 전체 1.014). 그런데 **S★가 빠질 때 같이 빠진다**
#      (S★ 최악20% −3.48bp/일 vs 최고20% +3.18bp/일) — 칼마가 떨어지는 이유가 여기다.
#    뉴스 대조 — 최악 3구간 모두 **추세에 선행 경고가 없던 사건**:
#      2026-07 SOX −21%(2008-10 이후 최악): 6/30 Burry 공매도 공개 → 7/16 Kimi K3 오픈소스 → AI 펀드 마진콜.
#        단 **변동성 지문은 있었다**(7월 거래일 약 50%가 4% 이상 이동 · 22거래일 전부 장중 2% 이상 스윙).
#      2026-02: 1/30 Warsh 지명 → 1/31 AI 대체 내러티브(소프트웨어 하루 −6%) → 2/4~5 고용 부진. "사전 경고 없이 갑자기".
#      2024-08-05: 엔 캐리 청산.
#    → 공통 지문은 변동성이지만 **시점이 갈린다**(실측): vol21 **하위**20%가 향후 21일 하락을 맞추고
#      (연도비율 0.650 · 정밀−기저 +0.0593 · 25/29 산업), **상위**20%는 못 맞춘다(0.396 · −0.0198).
#      즉 저변동성 = 하락의 **사전** 조건, 고변동성·깊은 낙폭 = 하락이 **진행 중**. 도구가 둘로 나뉜다.
#
#  ⚠⚠ 기본값 변경 1건 — **위험 파라미터**(사용자 규칙 §7에 따라 명시):
#    **R1 — I.INDUSTRY_LEADER_BRAKE 신설, 기본 True.** 보유 중인 **리더 산업**이
#      (a) 자체 dd63(63일 고점 대비 낙폭) ≤ 자기이력 BRAKE_DD_Q(0.10) 분위  **또는**
#      (b) park5(고가−저가 5일 변동성) 자기이력 백분위 ≥ BRAKE_VOL_Q(0.90)
#      이면 그날 그 산업 몫을 **부모 ETF로 되돌린다**. ⚠ 현금화가 아니다 — 총노출·14_계층정합 불변.
#      **근거는 수익률이 아니라 correctness(D1)**다. 13j 청산 사유에 '하락 브레이크'를 신설(D2 해소).
#      반사실 추정(⚠ 리포트 재구성이라 **연율 0.21%p 잔차** — 산업 다리 +0.642%p의 1/3):
#        MDD −0.1113 → −0.1060 · 칼마 3.318 → 3.479 · CAGR −0.0006.
#        그래서 **정확한 수익곡선 판정은 엔진 격자(R2)에 맡긴다** — 근사로 채택하지 않는다.
#      ⚠ 되돌리기: i_overrides={"INDUSTRY_LEADER_BRAKE": False}
#
#  신규 진단·격자 4건:
#    (R2) **[리더위험격자]** — 브레이크 변형 3행(dd10만 / park0.90만 / dd20+park0.90)과 각 행의
#      **달력 대조군**(같은 산업·같은 날수를 **신호 없이 균등 간격**으로 끔). 대조군이 있어야
#      "브레이크가 좋은 날을 골랐나"와 "그냥 노출을 줄여 좋아졌나"를 가를 수 있다(CAP 0.25도 칼마 3.584였다).
#    (R3) **신규 시트 18_리더위험진단** — 리더 에피소드 전수의 비중변화·낙폭변화·최저낙폭·점수변화·H변화·
#      제동일수·**병리 플래그**+ 요약(점수 오름 비율·H 내림 비율). D1이 세 라운드 안 보였던 이유가 이 표의
#      부재이므로 앞으로 매 실행 드러나게 한다.
#    (R4) **13p 블록 A3 + 01_일별 4열**(낙폭63일 · park5 자기이력pct · vol21 자기이력pct · 리더 하락 브레이크 ·
#      하락경고(진단)). 블록 A3는 하락 경고 후보를 **연도 k/n과 향후수익 격차**로 줄세운다.
#      실측: 경고일 향후21일 +0.267% vs 비경고 +1.239%(격차 −0.972%p) · 하락비율 0.4982 vs 0.4136.
#      ⚠ **라이브 신호로 쓰지 않는다** — ⑥&vol33이 연도 5/6이지만 2021·2025 두 해가 끌고 **2024년 격차가
#        −0.22%p로 사실상 0**이다. 얇은 근거로 신호층을 바꾸지 않는다(v0.14.0 C4의 교훈).
#    (R5) OHLC 변동성 열은 전부 rolling/expanding만 쓴다 — 절단재계산으로 룩어헤드 0 확인(테스트).
#
#  실행은 한 번이면 된다(S·M 무변경):  out = RP.main()
#
#  ★ 이번 실행에서 볼 것 — 사전 고정(R1은 엔진 실측, R4는 연도 k/n):
#    1) **R1 판정 ①②③(가장 중요 · 세 개 다 통과해야 라이브 유지).** 13f_산업수용기준에서
#       · ② MDD 악화 ≤ **0.010** (보고서13 실측 0.0158 → 개선 필요)
#       · ⑤ 칼마 **> S★ 3.758** (보고서13 실측 3.328)
#       · ① CAGR 손실 ≤ 0.5%p → I★ CAGR ≥ **0.3654** (보고서13 0.3704)
#       하나라도 미달이면 i_overrides={"INDUSTRY_LEADER_BRAKE": False}로 되돌리고 [리더위험격자] 실측을
#       근거로 재설계한다. ⚠ 브레이크는 노출을 줄이므로 CAGR이 약간 내려가는 것은 정상이다 — 문제는 칼마다.
#    2) **R1 판정 ④ 브레이크가 실제로 일했는가.** 13j_배분거래내역 청산 사유에 **'하락 브레이크'가 ≥ 5건**.
#       0건이면 문턱이 너무 엄격하거나(BRAKE_DD_Q·BRAKE_VOL_Q) 제동일이 리더 보유일과 겹치지 않는 것이다 —
#       로그 event=leader_brake_live의 brake_industry_days와 13c의 리더 열을 대조할 것.
#    3) **R1 판정 ⑤ 병리가 줄었는가.** 18_리더위험진단 블록 B의 '★ 병리 에피소드'가
#       **6개 → ≤ 2개**여야 한다. 그리고 SOXX 2024-06~08 행의 '제동일수'가 0이 아니어야 한다
#       (그 구간을 끊었다는 직접 증거 — 이게 이번 라운드의 핵심 확인 항목이다).
#    4) **R2 달력 대조군(필수).** 13_산업배분전략에서 `리더브레이크 … [리더위험격자]` 각 행이
#       같은 이름의 `대조: … 같은날수 달력(신호없음)` 행을 **칼마와 MDD 둘 다에서** 이겨야 채택 후보다.
#       대조군을 못 이기면 "신호가 좋아서"가 아니라 "노출을 줄여서" 좋아진 것이므로 브레이크를 되돌린다.
#    5) **R4 블록 A3.** 13p에서 A3-1~A3-5의 '연도 정밀도>기저'·'연도 격차음수'·'2024 격차(%p)'를 볼 것.
#       기준선(⑥ 현행)은 연도비율 0.46 근처가 정상이다. 승격 조건은 **연도비율 ≥ 0.60 그리고
#       연도 격차음수 ≥ 6/8 그리고 2024 격차 < 0** — 셋을 다 만족한 행이 있으면 다음 라운드에 라이브 검토.
#    6) **01_일별 4열.** '낙폭63일'·'park5 자기이력pct'·'vol21 자기이력pct'·'리더 하락 브레이크'·
#       '하락경고(진단)'. SOXX 2024-07~08 행에서 낙폭63일이 −0.25 근처까지 가는 동안 '리더 하락 브레이크'가
#       '제동'으로 찍히는지 눈으로 확인할 것(D1의 직접 반증).
#    7) **시트 전수.** 13·13b·13c·13c2·13f·13j·13l·14·15·16·17 + 신규 **18_리더위험진단**.
#    8) **감사 0건(필수)** — 11_룩어헤드감사·11b_순환매신호감사 불일치 0. R1/R4의 신규 열은 rolling/expanding
#       만 쓰므로 영향이 없어야 한다.
#    9) **v0.14.0 성과 보존 확인** — 13l 리더일이 여전히 > 0 (2,903일에서 브레이크만큼 줄어드는 것은 정상,
#       0이 되면 안 된다) · 13g 리더판단사용 ≥ 2인 연도 유지 · 17 블록 A 단조 유지.
#   10) **S·M 무변경** — S★ 35.87/2.213/−9.55 · M★ 25.50/2.236/−7.07 · 섹터 격자 33행.
#   11) **14_계층정합 위반 0** — 브레이크는 부모 ETF로 되돌리므로 총노출이 변하면 안 된다(불변식 검사).
#
#  [검증] 이 세션에는 시장데이터·정식 회귀 스위트가 없어 위 11항목을 실제로 실행하지 못했다. 한 것:
#    (1) ast.parse·import 성공, IndustryConfig 기본값 확인
#    (2) 합성데이터 단위테스트 3종 **118 PASS / 0 FAIL**(test_i015 51 · test_i014 43 · test_i014_sheet17 24):
#        · R1 build_leader_risk_features: **절단재계산 동일(dd63·park5_pct·vol21_pct·brake 전부 최대차 0)**
#          · 심어둔 급락 구간에서 제동 발동 · 평온 구간 제동률 낮음 · 이력 부족 구간 무제동
#          · BRAKE_USE_DD/USE_VOL/분위 스위치가 실제로 제동일을 바꾼다 · OHLC 없을 때 폴백 · 상수가격 0분모 방어
#        · R3 build_leader_risk_diagnosis: 설계한 병리(비중 0.10→0.30 & 낙폭 −0.0305)를 '⚠ 예'로 잡고
#          비중 일정 에피소드는 잡지 않음 · 낙폭 깊은 순 정렬 · 요약/판정/면책/달력대조군 경고 행 · 빈 입력 방어
#        · R4 블록 A3: 판정 열 전부 · 기준선 행 동반 · 승격조건 행 · 정밀−기저 산술 일치 · 빈 입력 방어
#        · v0.14.0 회귀 가드 전부 통과(순서기반 채택·결합국면·17 시트·연도판정)
#    (3) 검증 중 실제로 고친 버그 1건: 블록 A3에서 object dtype 열에 `~`를 써 **비트 부정(-1/-2)**이 되어
#        .loc 인덱싱이 KeyError로 죽었다 → astype(bool) 3곳 추가.
#    ⚠ **반사실 한계를 명시한다**: R1의 MDD/칼마 추정치는 리포트만으로 재구성한 것이고 검증 결과
#      연율 **0.2065%p**의 잔차가 남았다(최적 정렬에서도). 산업 다리 전체가 +0.642%p/년이므로 오차가 1/3이다.
#      그래서 이번 라운드는 수익곡선을 **주장하지 않고** 엔진 격자(R2)에 판정을 맡긴다.
#    **로컬에서 반드시 필요한 것**: 위 11항목 실측, 회귀 스위트 재실행 + 기본값 가드 갱신
#    (INDUSTRY_LEADER_BRAKE=True · ROTATION_ADOPT_BY="order" · COUPLING_STATE=True),
#    그리고 **13l 리더일 > 0 회귀 테스트**(v1.10.0에서 권고한 것 — 아직 없다면 이번에 만들 것).
#
#  ---- v1.10.0 원문(참고) ----
#  VERSION: v1.10.0 - 2026-09-14 - [문서 + 실행 레시피 — 실행 로직 무변경] **I v0.13.0 → v0.14.0만** ·
#                    S v0.48.0 · M v1.53.1 무변경. REPORT53 구현분 — 산업(I)만 고친다.
#
#  사용자 지시: "결과인데 전혀 나아진게 없어 각 산업별로 부모 섹터를 따라가는 경우는 언제이고 반대로
#  움직이는 경우는 언제인지, 어떤 조건인지 파악해서 예측해야돼 그게 왜 안되는건지 원인 찾고 개선방법
#  찾아서 수정해봐"  → 보고서 12(v0.13.0 산출)에서 원인 4개를 특정하고 그 4개를 고쳤다.
#
#  ⚠ 왜 v0.13.0이 "전혀 나아진게 없"었나 — 보고서 12 실측 4가지:
#    (C1 ★치명) **9년 동안 산업 리더가 단 하루도 없었다.** 13l: "총 0회 — 리더가 한 번도 나오지 않았다"
#      (채택신호 없음 21,850일 · 국면 게이트 차단 10,445일). 13g 연도별 채택/리더판단사용 =
#      2018:1/0 2019:1/0 2020:0/0 2021:1/0 2022:1/0 2023:0/0 2024:0/0 2025:0/0 2026:1/0.
#      채택된 5년은 전부 P_REL_VOL_RATIO 단독('최선 가용(t≥1.0, 상위2)')이고 곧바로
#      "미사용(교차확인 불가 — 사용 신호 1개 < 필요 2개)"로 폐기됐다. 신호를 고쳐도 **선택이 없으니**
#      결과가 변할 수 없었다. 13f도 ①CAGR·④스프레드·⑤칼마가 전부 실측 0으로 FAIL이었다.
#    (C2 ★) 채택 게이트가 크기(t)로 재서 순서를 버린다 — 2018 학습창: 순서가 무작위보다 나은 신호 **16/16**
#      (0.392~0.450 vs 무작위 0.327), |t| ≥ 1.0 은 **4/16**. t가 통과시킨 P_REL_VOL_RATIO는 풀링 상위3−EW29
#      = −0.071로 **부호 반전** 신호다. 원칙 재확인: "t는 크기의 진술이지 순서의 진술이 아니다".
#    (C3) N2(M상속)은 설계대로 작동했지만 **산업 고유정보를 0으로** 만들었다 — 01_일별 섹터상황 분포가 M과
#      비트 동일(1340/618/227), 13p ⑥ ≡ ⑨ 정확히 일치, 같은 날 29산업 예측의 **단면 상이도 0.0000**.
#      시장 신호 하나를 29번 복사한 것이라 "SOXX 하락을 맞춰라"는 구조적으로 답할 수 없었다.
#    (C4 ★판정 잘못) **v1.9.0의 사전등록 문턱이 풀샘플이었다.** ⑥은 문턱을 전부 통과했다(29평균 MCC 0.0842
#      ≥0.06 · 기술6 0.1147 ≥0.09 · SOXX 0.1639 ≥0.12 · 재현율 0.4338 ≥0.35 · 균형 0.5414 >0.53).
#      그런데 연도로 쪼개면 정밀도>기저가 h=21 **4/8년**, h=63 **2/8년** — 동전던지기다. 상관 높은 29계열을
#      6.3만 행으로 세면 유효표본이 '시장 1계열 × 9년'에 가까운데도 MCC가 크게 나온다. **사용자 말이 맞고
#      문턱이 틀렸다.** 그래서 v0.14.0은 판정 기준 자체를 바꾼다(P4).
#
#  ⚠ 기본값 변경 1건 — 산업(I) 계층, 신호층:
#    ⚠⚠ **P1 — I.ROTATION_ADOPT_BY 신설, 기본 "order"**(종전 동작 = "t"). 부모 안 신호 채택·정렬을 t 대신
#        **순서여유**(= 13g '학습창 1위=부모안 실현1위 비율' − '무작위 기대(1/부모안 산업수)')로 하고 상위
#        ROTATION_ORDER_TOP_K(=3)개를 채택한다. 새 통계를 만들지 않는다 — 13g가 이미 내던 값을 채택에 연결할 뿐이다.
#        교차확인(ROTATION_MIN_AGREE=2)은 **그대로 둔다** — 3개가 채택되므로 정족수가 비로소 성립한다
#        (우연 거르기 목적은 유지, '구조적 불가능'만 제거). v0.10.0 H3가 퇴역시킨 정족수 면제 격자
#        (INDUSTRY_LEADER_STANDALONE_EDGE)는 **켜지 않는다** — 그때 진단대로 문제는 정족수가 아니라 채택 잣대였다.
#        시뮬레이션 실측(워크포워드 · 사전등록 부호 고정 · 부모초과 21일):
#          리더일 **0 → 연 888~1,831일** · 부모안 적중 **0.378 vs 무작위 0.343 — 8/8년 초과**
#          리더 초과 **+0.209%** vs 타산업 **−0.120%**(차이 +0.329%p, 5/8년)
#        ⚠ 판정은 **순서**로 한다 — 수익 차이는 5/8년이라 판정 잣대로 쓰지 않는다(C4 교훈).
#        ⚠ 되돌리기: i_overrides={"ROTATION_ADOPT_BY": "t"}   (안전판만 원하면 ROTATION_ORDER_T_FLOOR=1.0)
#
#  신규 진단 3건(신호·배분에 영향 없음):
#    (P2) **결합국면** — I.COUPLING_STATE(기본 True). 산업↔부모 결합도를 corr63 · 특이변동성비중 · 부모변동성의
#      **자기이력 백분위** 평균으로 만들어 강결합/보통/탈동조 3상태로 나눈다. 01_일별_<산업>에
#      '결합점수백분위'·'결합국면' 2열 추가. ⚠ 자기이력 백분위가 핵심 — 원값 단면 비교는 GDX 0.42 / KBE 0.93
#      같은 정적 정체성으로 줄세우는 셈이다(실측: corr63 원값 → fi21 상위3−하위3 5/9년, 자기이력 백분위 **7/8년**).
#      되돌리기: i_overrides={"COUPLING_STATE": False}
#    (P3) **신규 시트 17_부모추종조건** — 사용자 질문에 직접 답하는 시트. 블록 A 결합국면 × M국면 조건부
#      추종/역행률(h=5/21/63) · 블록 B 산업별 역행(소수 클래스) 정밀도·재현율·MCC + **연도 정밀도>기저 k/n** ·
#      블록 C **부호반전표** · 블록 D 해석·사전등록 문턱. 진단 전용.
#    (P4 ★) **판정 기준 변경 — 연도별 부호일치를 1급으로.** 13p 블록 A2 모든 규칙 행에 '연도 정밀도>기저'·
#      '연도 MCC>0' (k/n) 열을 추가하고, 00_실행요약의 국면 판정이 **풀샘플 MCC가 아니라 연도 k/n**을 본다.
#      새 합격선: **연도×산업 정밀도>기저 비율 ≥ 0.60**. 풀샘플 MCC는 보조 지표로 강등.
#    (P5) 16_산업부모추종 **정직화** — 전이력 열 이름에 '(정적)' 표시(예측에 쓸 수 없다는 뜻) + 시변·인과 열
#      5종 추가(결합점수 중앙값 · 강결합/탈동조 일비중 · 두 구간의 부모초과 연율%).
#
#  실행은 한 번이면 된다(S·M 무변경):  out = RP.main()
#
#  ★ 이번 실행에서 볼 것 — **사전 고정 · 전부 연도 k/n 기준**(C4 재발 방지):
#    1) **P1 판정 ① 리더가 나오는가(가장 중요).** 13l_산업리더적중률이 더 이상 "총 0회"가 아니어야 한다.
#       · 13l C블록 '리더 에피소드' 총 회수 > 0, A블록 '리더일' > 0 인 연도 **≥ 7/9**
#       · 13g '리더 판단 사용'이 '사용'인 신호가 **≥ 2개인 연도 ≥ 7/9**(종전 9/9년 0개)
#       미달이면 ROTATION_ORDER_TOP_K를 4~5로 올려 보고, 그래도 안 되면 국면 게이트
#       (INDUSTRY_LEADER_REGIMES) 차단 일수를 13l B블록에서 확인할 것 — 종전 차단 10,445일이었다.
#    2) **P1 판정 ② 순서에 값이 있는가.** 13l A블록 h=21: '리더=부모안 실현1위' > 무작위 기대인 연도 **≥ 7/9**,
#       그리고 **'리더 평균초과' > '타산업 평균초과'**. 기대값: 적중 0.378 vs 무작위 0.343, 초과 +0.209% vs −0.120%.
#       ⚠ CAGR·MDD·칼마(13f ①⑤)는 이번 판정에 **쓰지 않는다** — 수익은 5/8년이라 잣대가 못 된다.
#       미달이면 i_overrides={"ROTATION_ADOPT_BY": "t"}로 되돌리고 13g 순서여유 열을 근거로 원인을 적을 것.
#    3) **P1 부작용 확인.** 13g '채택 근거'가 '순서 채택(여유 +0.0xx, 상위3)'으로 찍히고, 채택 신호가 더 이상
#       P_REL_VOL_RATIO 단독이 아니어야 한다(2018 학습창 기대: SCORE_PCT · SCORE_MINUS_PARENT · P_REL_MOM_63).
#       ⚠ SCORE_* 의 사전방향은 이미 −1로 등록돼 있다(부모 안 평균회귀) — 부호를 고친 게 아니라 쓰이게 한 것이다.
#       독립 재현: score_gap → 절대수익 상위3−하위3 = **−1.133%p/21일, 9년 중 0년만 양수**.
#    4) **P2 결합국면.** 01_일별_<산업>에 '결합점수백분위'·'결합국면' 2열이 있고, 29산업 각각에서 강결합/보통/
#       탈동조가 모두 나타나야 한다(한 상태로 쏠리면 COUPLING_MIN_HIST·창 길이를 의심). 로그 event=coupling_state_ready.
#    5) **P3 판정(17_부모추종조건).** ⑤ 블록 A h=21에서 **탈동조 역행률 > 강결합 역행률**이 세 M국면 중 최소 2개에서
#       성립(기대 방향: 강결합 낮음 → 탈동조 높음, 단조). ⑥ 블록 B 29산업 평균 '연도 정밀도>기저' **≥ 5/8**.
#       ⑦ MCC>0 산업 **≥ 18/29**(기대 21/29). 기저(역행률 h=21) 기대 ≈ 0.197.
#    6) **P3 블록 C 부호반전(사용자 질문의 답).** '부모베타 63일(자기이력 백분위)' 행에서 단면IC가
#       M상승아님 **양수** / M상승 **음수**로 갈리는지(기대 +0.051 / −0.038, 전체는 −0.003으로 상쇄) —
#       '부호반전' 열에 '⚠ 반전'이 찍혀야 한다. 이게 "언제 따라가고 언제 반대로 가는가"의 핵심 답이다.
#    7) **P4 판정 기준 확인(자기점검).** 13p 블록 A2 ⑥ 행의 '연도 정밀도>기저'가 **4/8 근처**로 보고되어야 한다
#       — 보고서 12의 재현이며, 이 값이 낮게 나오는 것이 **정상**이다(그게 C4의 증거다). 여기서 갑자기
#       7/8 같은 값이 나오면 P4 집계가 잘못된 것이니 _yearly_consistency의 min_days와 분모를 확인할 것.
#       새 합격선(연도×산업 비율 ≥ 0.60)은 ⑥이 아니라 **다음 라운드의 새 규칙**에 적용한다.
#    8) **P5 16_산업부모추종.** 전이력 열 이름에 '(정적)'이 붙고, '강결합 일비중'·'탈동조 일비중'·
#       '강결합/탈동조 구간 부모초과(연율%)' 열이 채워져야 한다. SOXX 기대: 부모 하락국면 초과 +33.40%(v0.13.0 실측)
#       과 결합국면별 값이 함께 보이면 정적/시변 대비가 성립한다.
#    9) **시트 전수(N1 규약 유지).** 13_산업배분전략·13b·13c_일별배분비중·13c2·13f·13j·13l·14·15 **전부** +
#       신규 **17_부모추종조건**. 동결로 13·13c를 없애지 않는다는 규약은 그대로다.
#   10) **감사 0건(필수)** — 11_룩어헤드감사·11b_순환매신호감사 불일치 0건. P1은 채택 규칙만 바꾸고
#       P2는 rolling/expanding만 쓰므로 감사 대상(복합점수·H·rot_raw)에 영향이 없어야 한다.
#   11) **S·M 무변경 확인** — S★ 35.87/2.213/−9.55 · M★ 25.50/2.236/−7.07 · 섹터 격자 33행 그대로.
#       ⚠ I★는 P1로 **바뀐다**(리더가 생기므로) — 14_계층정합 위반 0건만 확인하고, I★≡S★ 동일성은
#       이번 라운드에 기대하지 않는다(그게 P1의 목적이다).
#
#  [검증] 이 세션에는 시장데이터·정식 회귀 스위트가 없어 위 항목을 실제로 실행하지 못했다. 한 것:
#    (1) ast.parse·import 성공, IndustryConfig 기본값 확인
#    (2) 합성데이터 단위테스트 2종 **67항목 전부 통과**(test_i014.py 43 + test_i014_sheet17.py 24):
#        · P2 build_coupling_state **절단재계산 동일(룩어헤드 0)** · 0~1 범위 · 최소관측 NaN · 0분모 방어
#        · P4 _follow_metrics MCC ±1 경계·수기 일치·0분모 NaN, _yearly_consistency 표본미달 연도 분모 제외
#        · P1 순서 경로를 **보고서 12의 2018 학습창 실측 16신호로 재현** — 채택 3개로 정족수 성립(C1 해소),
#          P_REL_VOL_RATIO 단독 채택 사라짐, t 경로는 1개만 통과해 정족수 미달(C1 기계적 원인 재현),
#          MIN_EDGE·T_FLOOR·min_days 문턱이 실제로 작동, 되돌리기 스위치 동작
#        · P3 17 시트 4블록 생성 · 탈동조>강결합 역행률(h=5/21/63 전부) · 추종률+역행률=1 · 연도 k/n 열 존재
#          · 결합국면 없을 때 '산출 불가' 1행 안전 반환 · 빈 results·M국면 없음에서 예외 없음
#    (3) 검증 중 실제로 고친 것 2건: 단면 최소폭 하드코딩 12 → 유니버스 비례(소규모에서 블록 C가 조용히
#        비는 문제), 추종률을 반올림된 역행률에서 빼도록 수정(독립 반올림 시 합이 1.0001이 되는 문제).
#    **로컬에서 반드시 필요한 것**: 위 11항목 실측, 회귀 스위트 전종 재실행 + 기본값 가드 갱신
#    (ROTATION_ADOPT_BY="order" · COUPLING_STATE=True), 13l이 0회가 아님을 고정하는 회귀 테스트 신설
#    (이게 다시 0이 되면 산업 계층 전체가 조용히 죽는다 — 세 라운드를 그 상태로 보냈다).
#
#  ---- v1.9.0 원문(참고) ----
#  VERSION: v1.9.0 - 2026-09-14 - [문서 + 실행 레시피 — 실행 로직 무변경] **I v0.12.0 → v0.13.0만** ·
#                    S v0.48.0 · M v1.53.1 무변경(사용자 지시 "industry만 수정"). REPORT52 §6 N1~N6 구현분.
#
#  ⚠ 기본값 변경 2건 — 둘 다 산업(I) 계층:
#    (1) **N1 — I.INDUSTRY_LAYER_FROZEN True → False**(해제) + **동결의 의미 축소**. v1.8.0(경로 A)의 동결이
#        13_산업배분전략(전략별 수익)·13b·13c_일별배분비중·13c2·13f·13j·13l·14·15를 통째로 없앴고 사용자가
#        "왜 산업 전략별 수익, 일별배분비중 시트 또 없앴어? 다시 생기게 해"라고 지적했다(R46에 이어 두 번째).
#        이제 동결이어도 배분을 **격자만 꺼서** 만들기 때문에(_frozen_alloc_cfg) 13·13b·13c·13c2·14는 항상
#        나오고, 생략되는 것은 격자 행과 13f·13j·13l·15뿐이다. 규약에 고정: 동결로 13·13c를 없애지 않는다.
#        되돌리기(동결): i_overrides={"INDUSTRY_LAYER_FROZEN": True}
#    (2) ⚠⚠ **N2 — I.INDUSTRY_REGIME_SOURCE 신설, 기본 "m_inherit"**(신호층 변경, 이번 라운드의 유일한 ⚠⚠).
#        산업 확정국면·목표비중을 **M(SPY) 국면에서 상속**한다(M 상승→상승 1.0 / 중립→중립 0.5 / 하락→하락 0.0).
#        근거(REPORT52 §2~4 원자료 실측, h=21 하락 소수클래스 MCC — 29산업 평균/기술6/SOXX):
#          자기 라벨 0.007 / 0.057 / 0.060(13p 실측 29평균 **0.004** · 재현율 0.130) · 자기 현금 0.028 · 자기
#          점수 하위10% 0.026 · 자기 H>0.7 0.014 · 부모 H>0.7 0.028 · **M 상승아님 0.075 / 0.106 / 0.154**
#          (정밀 0.477 · 재현 0.482 · 균형 0.537 · 27/29 양수) · ⚠ M '하락' 라벨만 −0.004(정보 없음).
#        2022년엔 29산업 중 25개가 하락 예측 정밀도 < 기저율(늦었다), 2024년(SOXX −25%)엔 16개 산업이 하락
#        예측 0일, SOXX는 급락 중 자기 점수 백분위가 0.30→0.93으로 역행했다. 원인은 신호가 아니라 구조다 —
#        산업 복합점수의 채택 지표가 대부분 매크로(SOXX PASS 21개 중 19개)이고 H·급락트리거는 이미 SPY 것을
#        공유하므로 산업 고유 하락 정보가 없다. **자기 기계 값은 버리지 않는다**: 01_일별_<산업>에 '국면 소스'·
#        '자기국면'·'자기 목표비중' 3열, 01Z에 '상승/하락예측 산업수(자기국면)' 2열, 13p 블록 A2 ①②③ 행으로
#        보존돼 매 실행 비교된다. 사전등록(기본 꺼짐) "m_inherit_own_down"(자기 하락일은 하락 유지, 실측
#        0.072 / 0.105 / 0.154)은 다음 라운드가 블록 A2에서 ⑥ vs ⑦로 판정한다.
#        ⚠ 되돌리기(v0.12.0 동작): i_overrides={"INDUSTRY_REGIME_SOURCE": "own"}
#    (N3·N4 진단) 13p에 **블록 A2 '국면 라벨 비교(하락 경고)'** 신설 — 규칙 10종 × 산업 × (예측일·정밀도·
#      재현율·균형정확도·MCC) + 29산업/기술6 평균 + **연도별 요약**(하락 예측일 합·예측 0일 산업 수·정밀도<기저
#      산업 수). 또 S.build_minority_class_accuracy에 M 국면·목표비중을 합성 dict로 넘겨 블록 A의 'M상속'
#      열(종전 산업에서는 내내 비어 있었다)도 채운다 — S 코드는 손대지 않았다.
#
#  실행은 한 번이면 된다(S·M은 무변경이라 대조 실행이 필요 없다):  out = RP.main()
#
#  ★ 이번 실행에서 볼 것(REPORT52 §7 표 7항목):
#    1) **N1 — 시트 복구(필수).** 산업 리포트에 13_산업배분전략(격자 포함 14행 안팎)·13b_배분전략자산곡선·
#       13c_일별배분비중·13c2_잔여다리·13f_산업수용기준·13j_배분거래내역·13l_산업리더적중률·14_계층정합·
#       15_산업대섹터귀속이 **전부** 있어야 한다(기본값이 해제이므로 격자·13f까지 나온다).
#    2) **N2 판정(사전 고정) — 13p 블록 A2 ⑥ '확정 상승아님' 행(h=21)**: 29산업 평균 MCC ≥ **0.06**(기대
#       0.075) · 기술 6종 ≥ 0.09(0.106) · SOXX ≥ 0.12(0.154) · 재현율 ≥ 0.35(0.43) · 균형정확도 > 0.53(0.537).
#       미달이면 i_overrides={"INDUSTRY_REGIME_SOURCE": "own"}으로 되돌리고 원인을 적을 것.
#       로그 한 줄로도 찍힌다: event=regime_label_block_ready cur_mcc=… cur_mcc_tech=… verdict=…
#    3) **N2 상속 검증** — 블록 A2에서 ⑥ ≈ ⑨(M 상승아님 원천)·⑤ ≈ ⑩(M 현금 원천)이어야 한다(상속이 제대로
#       됐다는 증거). 그리고 ① 자기 라벨 행이 v0.12.0 값(29평균 0.004·SOXX 0.055)을 재현해야 한다 —
#       상속이 자기 기계 진단을 덮어쓰지 않았다는 증거. 로그 event=industry_regime_inherited changed_days=…
#    4) **N4 연도별 요약 행** — '자기: 예측 0일 산업 수'가 2024년 16 근처로 재현되고, '확정: 예측 0일 산업
#       수'는 0이어야 한다(M 상승아님은 매년 있다). 2022년 '자기: 정밀도<기저 산업 수' ≈ 25.
#    5) **01_일별_<산업> 3열 · 01Z 2열** — '국면 소스'='M 상속(m_inherit)'·'자기국면'·'자기 목표비중',
#       01Z의 '상승/하락예측 산업수(자기국면)'가 종전 산업별 차이를 그대로 보여 주는가.
#       그리고 01Z 마지막 행(다음 거래일 예측)의 산업 예측이 **M 상태와 일치**하는가(2026-09-11 기준 상승).
#    6) **I★ ≡ S★ · 14_계층정합 위반 0일(필수)** — N2는 적격 판정(state=="RISK_ON")을 M 상승일로 바꾸지만
#       리더는 여전히 교차확인(채택 0~1개)으로 구조적 불가라 산업 배분 0일이 유지된다. 13_산업배분전략의
#       ★ 행이 S★ CAGR 35.87% / 샤프 2.213 / MDD −9.55%와 같아야 하고, S★ 재현 행은 대조군A와 비트 동일.
#       ⚠ 만약 산업 배분일이 0일이 아니게 되면 I★가 S★에서 갈라진다 — 그때는 로그 event=allocation_perf로
#       원인을 확인하고 보고할 것(예상되지 않는 결과다).
#    7) **감사 0건(필수)** — 11_룩어헤드감사·11b_순환매신호감사 불일치 0건. N2는 라벨만 바꾸므로 감사 대상
#       (복합점수·H·rot_raw)에는 영향이 없다.
#    8) **S·M 무변경 확인** — S★ 35.87/2.213/−9.55 · M★ 25.50/2.236/−7.07 · 섹터 격자 33행 그대로.
#
#  [검증] 이 세션에는 시장데이터·정식 회귀 스위트가 없어 위 항목을 실제로 실행하지 못했다. 한 것:
#    (1) py_compile·import 성공 (2) 신규 로직 3종(N2 inherit_industry_regime · N1 _frozen_alloc_cfg ·
#    N3/N4 build_industry_regime_label_block)을 합성데이터로 자체 단위테스트 **35항목 전부 통과**
#    (M 중립일 상속·min(M,자기)·ffill 인과성·입력 불변·MCC/정밀/재현 수기 일치·⑥=⑨·①=④(own 등가)·
#    연도별 집계·격자 전부 꺼짐·원본 cfg 불변) (3) 시트 게이트를 코드 추적으로 확인.
#    **로컬에서 반드시 필요한 것**: 위 8항목 실측, 회귀 스위트 전종 재실행 + 기본값 가드 갱신
#    (INDUSTRY_LAYER_FROZEN=False · INDUSTRY_REGIME_SOURCE="m_inherit"), test_industry_v07_minority에
#    블록 A2 존재·"own" 되돌리기 동등성 추가.
#
#  ---- 이전 라운드 레시피(참고) ----
#  VERSION: v1.8.0 - 2026-09-14 - [문서 + 실행 레시피 — 실행 로직 무변경] S v0.47.0 → v0.48.0 · I v0.11.0 →
#                    v0.12.0 · M v1.53.1(무변경). REPORT51 구현분 — S: L1 13p 블록B·E 지평열(h=42·63·126
#                    진단) · L2 [득표상실청산격자] · L3 [보유기간격자] · L4 00시트 리더/하락국면리더 귀속
#                    분리(⚠결함수정) · L5 13c '득표 상실 중 보유' 열 · L6 11b_순환매신호감사(rot_raw 절단
#                    재계산, S·I 공통 신설). I: M1 ABS_MOM_12_1 중복 후보 제거(⚠결함수정) · M2 13p 블록P
#                    최빈 부모 몫 분모 수정(⚠결함수정) · M3 v0.11.0 CHANGELOG 정정(rot_raw는 감사 대상이
#                    아니었다) · 경로 A(REPORT51 §7 — 산업 계층 재동결, 아래 참조).
#
#  ⚠ 기본값(신호·채택·사이징) 변경 1건 — 경로 A: I.INDUSTRY_LAYER_FROZEN 기본값 **False → True**(재동결).
#    REPORT51 §3.1 K7 사전 고정 문턱 둘 다 미달(① 순서·크기를 동시에 가진 풀링 전용 후보 없음 ② 슬리브가
#    형성일 일치 대조 2행과 구별 불가)이었다 — 사전 고정 규칙대로면 No-Go(경로 A)다. 보고서 §7이 권고한
#    경로 B(사후 규칙 변경으로 게이트를 순서 우선으로 바꿔 한 번 더 시도)는 시뮬레이션조차 ★를 넘지 못했고
#    (§3.3-c: f=0.1 CAGR 35.4/칼마 3.71 vs ★ 35.9/3.76) 보고서 스스로 "성과 개선이 아니라 순서 정보의 존재를
#    기록하는 것"이라 적었다 — 사용자가 결정을 위임했고("몰라 더 나은 개선방법으로 해"), 사전 등록 원칙을
#    깨면서까지 택할 값어치가 아니라고 판단해 경로 A를 골랐다(근거 전문은 industry_rotation.py
#    IndustryConfig.INDUSTRY_LAYER_FROZEN 필드 주석). 동결이어도 13g(풀링 5열+n)·13p·16은 그대로 나온다 —
#    생략되는 것은 배분·격자·수용기준(13·13b·13c·13c2·13f·13j·13l·14·15)뿐이다(≈40분 절약).
#    되돌리기: i_overrides={"INDUSTRY_LAYER_FROZEN": False}
#    나머지(S L1~L6, I M1~M3)는 전부 진단 열·배분층 격자(라이브 기본값 `()`로 꺼짐)·문서 정정·감사 신설이라
#    ★ 라이브 성과에는 닿지 않는다 — S★·I★(within-parent)는 v0.47.0/v0.11.0과 비트 동일해야 한다.
#
#  실행 순서(REPORT51 §5 "실행 순서" — 이번에도 한 번이면 된다, 신호·사이징 기본값 변경은 위 경로 A
#    하나뿐이고 그것도 산업 배분층을 끄는 방향이라 별도 대조 실행이 필요 없다):
#        out = RP.main()
#    (참고 — 개념상 순서는 S: L4+L5+L6(측정 정합) → L1(진단) → L2+L3(격자) → I: M1+M2+M3 → 경로A, 이지만
#     전부 같은 실행 안에서 함수 호출 순서로 이미 반영돼 있다 — 별도로 여러 번 돌릴 필요는 없다.)
#
#  ★ 이번 실행에서 볼 것(REPORT51 §6 "다음 실행에서 볼 것" 표 — 항목 7·8은 경로 B 전용이라 경로 A에서는
#    해당 없음, 대신 "경로 A 확인"으로 대체):
#    1) **L1 — 13p 블록 B·E 지평 열**: 교차 리더 611일 판단을 h=21 옆에 h=42·63·126으로 재면
#       h=21 0.43 / h=63 ≈0.53 / h=126 ≈0.64 부근으로 재현되는가(±0.02). 00시트 순환매 요약 줄에 h=21·63이
#       나란히 찍히는가.
#    2) **L2 — [득표상실청산격자] 블록 E 행**: 리더 판단일 ≈586·적중 ≥ 0.44, 격자 4~5기준 통과 여부(라이브는
#       기본 꺼짐 — 판정만 본다). 13c '득표 상실 중 보유' 열(L5)이 새로 보이는가.
#    3) **L3 — [보유기간격자] 42/63 행**: 블록 E h=63 적중이 현행(21일 min_hold 기준) 0.53보다 나아지는가,
#       리더 교체 횟수가 34회보다 줄어드는가, 격자 4~5기준.
#    4) **L4 확인(⚠ 결함수정 재현)** — 00시트 순환매 요약 줄이 이제 '리더(교차)'와 '하락국면리더'로 갈려서
#       찍히는가(종전엔 두 유형의 합산값이 한 줄로 나가 귀속이 섞여 있었다).
#    5) **L6 — 11b_순환매신호감사 불일치 0건(S·I 둘 다) — 필수.** S는 build_rotation_raw_signals() 직접
#       입력만 절단재계산, I는 build_industry_rotation_raw_signals() 직접 입력만 절단재계산(둘 다 이 세션에서
#       합성데이터 단위테스트 통과 — 실제 이력으로는 미검증, 아래 [검증] 참조).
#    6) **M1 확인 — 13g 산업 풀링 열에 ABS_MOM_12_1이 더 이상 없음**(풀링 전용 후보 7종 → 6종, 학습창당
#       9→8행 또는 22→21행 부근으로 줄어드는지 확인 — 정확한 행수는 그 해 유효표본에 따라 달라질 수 있다).
#    7) **경로 A 확인** — 산업 리포트 00시트에 "⚠ 산업 계층 동결(v0.5.0 I-A)"가 **동결(기본값)**으로 찍히고,
#       13_산업배분전략·13b·13c·13c2·13f·13j·13l·14·15 시트가 **없어야** 한다(13g·13p·16·01Y·11·11b는 유지).
#    8) **★ 성과 세 계층 비트 동일 — 필수(REPORT51 §6-9)**: S★ CAGR 35.87% / 샤프 2.213 / MDD −9.55% ·
#       M★ CAGR 25.50% / 샤프 2.236 / MDD −7.07%(전부 무변경 — S L1~L6·I M1~M3+경로A는 신호·사이징을
#       건드리지 않는다). 어긋나면 즉시 알려 줄 것.
#
#  [검증] 이 세션은 실제 market_regime_trader.py(M)·시장데이터·프로젝트 정식 회귀 스위트
#    (test_sector_v0*.py·test_industry_v0*.py)가 없어 위 항목을 실제로 실행해 확인하지 못했다. 대신
#    (1) 세 파일 모두 ast.parse+import 성공 (2) sector_rotation.py 신규 로직 3종(L2 vote_loss 상태기계·L6
#    절단재계산 비교·L1 지평 열 집계)과 industry_rotation.py 신규 로직 2종(L6 builder·audit)을 합성데이터로
#    자체 단위테스트했다(세션 로컬 scratch, 모두 통과 — 인과 입력 0건 불일치, 의도적 룩어헤드 주입 시 정상
#    검출) (3) grep/코드 추적으로 M2 분모 수정이 13p 블록 P의 유일한 소스에 적용됐음을 확인했다. **실제
#    배포 전 로컬(Kaggle/Colab)에서 반드시 필요한 것**: 위 §6 표 전 항목의 실측 재현, 기존 회귀 스위트
#    전종 재실행, 스위트에 신규 기본값 가드(INDUSTRY_LAYER_FROZEN=True·ROTATION_EXIT_ON_VOTE_LOSS_GRID=()·
#    ROTATION_MIN_HOLD_GRID=()) 반영, test_sector_v046_block_e에 신규 격자 2종 존재 확인 추가.
#
#  ---- 이전 라운드 레시피(참고) ----
#  VERSION: v1.7.0 - 2026-09-13 - [문서 + 실행 레시피 — 실행 로직 무변경] S v0.47.0 · I v0.11.0 · M v1.53.1(무변경).
#                    REPORT50 구현분: J1 ⚠결함수정 13p 블록E like-for-like(라이브 611일 vs 격자 611일 분리) ·
#                    J2 [단독회피격자] 퇴역 · J5 블록B 최빈 리더(집중도) 열 · K1 ⚠결함수정 [산업슬리브격자]
#                    부모 계층 신호 구조적 제외 · K2 풀링 전용 후보 7종(S_REL_*·ABS_MOM_12_1·HAZ_PCT_OWN) ·
#                    K3 ⚠결함수정 [산업슬리브격자·대조] like-for-like(형성일 마스크) · K4 13g '풀링 학습
#                    관측일(n)' 열 · K5 13p 블록P 연도별 채택신호·최빈 부모 몫 열(xlk_share 대체) ·
#                    K6 문서 정정(REPORT49 §3.4 칼마/MDD 인공물 — 역사 기록은 보존, 정정만 병기).
#                    K7(Go/No-Go 동결 게이트, INDUSTRY_LAYER_FROZEN)은 **사용자 승인 전에는 코드로 넣지
#                    않는다** — REPORT50 §6 "결정요청(사용자)"가 문턱을 승인한 뒤 다음 라운드에서 별도 반영.
#
#  ⚠ 기본값(신호·채택·사이징) 변경 1건 — J2: S.ROTATION_AVOID_STANDALONE_GRID 기본값
#    ("order","size+order") → ()(격자 퇴역). 독립 측정 3회(13g·13p 블록E 회피→하위3 0.15·초과 +0.99%/21일,
#    전부 무작위보다 나쁨 — REPORT50 §2.3) 모두 "꼴찌를 피하라" 방향이 성립하지 않음을 확인했다. ⚠ 라이브
#    배분에는 원래도 이 격자가 관여하지 않았다(진단/격자 전용 스위치) — S★·I★ 성과는 이번에도 비트 동일.
#    되돌리기: s_overrides={"ROTATION_AVOID_STANDALONE_GRID": ("order", "size+order")}
#    나머지(J1·J5·K1·K3·K4·K5)는 전부 like-for-like 버그수정 또는 순수 진단 열 추가라 끄고 켜는 스위치가
#    없다(J1·K3은 상시 적용되는 수정, K1은 구조적 제외라 상시 적용 — 되돌리려면 코드 되돌리기만 가능).
#    K2(풀링 전용 후보 7종)만 예외적으로 끌 수 있다: i_overrides={"INDUSTRY_POOLED_ONLY_SIGNALS": ()}
#    (비우면 [산업슬리브격자]의 풀링 채택 후보가 K1 이전 상태로 좁아진다 — within-parent I★는 원래도 무관).
#
#  실행은 이번에도 한 번이면 된다(신호·사이징 기본값 변경은 격자 전용 J2 하나뿐):  out = RP.main()
#
#  ★ 이번 실행에서 볼 것(REPORT50 §5 "다음 실행에서 볼 것" 8항목):
#    1) **J1 — 13p 블록 E**: '라이브 기준' 행이 이제 611일(리더)로만 잡히고, 새로 분리된
#       '[★ 하락국면리더일]' 행이 264일·적중 0.500으로 따로 나오는가. 종전엔 라이브 행이 875일
#       (611+264 뒤섞임)로 찍혀 격자 변형 행(611일)과 like-for-like가 아니었다 — 이제 맞는가 확인.
#       그리고 블록 B '리더(교차) 판단일' 2026년 값이 **0일**로 바뀌는가(종전 "16일→1.000"은 전부
#       하락국면리더 override였다 — REPORT47~49가 인용한 그 숫자가 실은 교차 리더가 아니었다는 뜻).
#    2) **J2 확인** — 13_섹터배분전략에 [단독회피격자] 행이 **없어야** 한다(격자 행수 35 → **33행** 안팎).
#       13g '단독회피 자격(순서)' 열과 avoid_standalone_by_year 계산은 진단으로 그대로 남는다(제거 대상 아님).
#    3) **K1 — 로그 event=sleeve_excluded_parent_level**에 PARENT_SCORE_PCT 등이 찍히는가. 13g에 새 열
#       '부모 계층 제외(K1, 슬리브 채택 불가)'가 해당 이름에 Y로 표시되는가(같은 신호가 within-parent
#       채택 후보에서는 계속 그대로 쓰인다 — I★ 비트 동일 확인은 §8 참조).
#    4) **K2 — 13g 풀링 진단 5열**에 신규 후보 7종(S_REL_MOM_12_1·126·63·ABS_MOM_12_1·S_REL_DD_252H·
#       S_REL_VOL_RATIO·HAZ_PCT_OWN)이 '풀링 학습 관측일(n)'(K4)과 함께 t·부호 판정과 같이 실리는가.
#       이 신호들이 [산업슬리브격자]의 연도별 채택 후보에도 실제로 뽑히는지 로그 event=industry_sleeve_grid
#       의 'years' 필드에서 확인할 것(뽑히지 않으면 K2가 무효화됐던 종전 결함이 재발했다는 뜻).
#    5) **K3 — 13_산업배분전략 [산업슬리브격자·대조] 라벨**에 '형성일 n/N'이 찍히고, 그 n이 슬리브
#       본행([산업슬리브격자])의 실제 보유일수와 일치하는가(같은 형성일 마스크를 썼다는 증거).
#       종전(매일 f) 대조 행과 칼마·MDD를 비교해 격차가 줄어드는지 확인 — REPORT50 §3.2 기준선:
#       종전 대조 칼마가 실제보다 부풀려져 있었다(K6 참조).
#    6) **K5 — 13p 블록 P**: 연도별 행에 '채택 신호(그 해)' 열, '── 전체 ──' 행에 '형성일/전체'·
#       '최빈 부모 몫' 열이 새로 보이는가. REPORT50 §3.1 기준선(엔진 재현, PARENT_SCORE_PCT 시절):
#       형성 655일 중 IYZ(XLC) 570일·FDN(XLC) 549일·SOCL(XLC) 444일 — 셋 다 XLC 소속이라 '최빈 부모 몫'이
#       **크게** 나와야 종전 xlk_share=0.0(이 셋을 놓쳤던 결함, §4 E6)이 재현·수정됐다는 뜻이다. K1·K2
#       적용 후에는 채택 후보가 바뀌므로 이 구체 수치 자체가 재현될 필요는 없다 — '몫이 여전히 한 부모에
#       쏠려 있는가'만 확인. 상위3 적중이 무작위(3/29≈0.103)를 **넘는지**가 판정 기준 ②(K7 사전 조건 중 하나).
#    7) **11_룩어헤드감사 0건** — K2로 새로 추가된 rot_raw 키(S_REL_*·ABS_MOM_12_1·HAZ_PCT_OWN)도 절단재계산
#       대상에 자동 등록되는가(신규 항목이 감사망 밖에 있으면 안 된다).
#    8) **★ 성과 세 계층 비트 동일** — M v1.53.1(무변경) · S★ CAGR 35.87%/샤프 2.213/MDD −9.55%(무변경,
#       J1·J2·J5 전부 진단/격자 전용) · I★ 무변경(INDUSTRY_SLEEVE_SHARE=0.0 그대로, K1~K5는 슬리브 채택
#       후보군·진단열만 건드린다 — within_parent_walkforward_select의 rank_full·ROTATION_SIGNALS는
#       손대지 않았다). 어긋나면 즉시 알려 줄 것.
#
#  ⚠ K7(Go/No-Go 동결 게이트) — REPORT50 §6 "결정 요청(사용자)". **이번 실행이 자동으로 판정하지 않는다.**
#    이번 실행 결과를 아래 문턱과 사람이 대조해 승인한 뒤에만, 다음 라운드에서 INDUSTRY_LAYER_FROZEN 반영을
#    코드로 넣는다(사후 적합 방지 — 문턱은 이 라운드 실행 전에 사전 고정한다):
#      ① 풀링 전용 후보(K2) 중 풀링 t ≥ 2.0이 9개 학습창 중 ≥ 5개 **그리고** 풀링 1위→실현 상위3 ≥ 0.15인
#         신호가 1개 이상 있는가 (13g 풀링 5열 + K4 'n' 열로 판독)
#      ② [산업슬리브격자] 후보 행이 **형성일 일치 대조 2행 모두**(K3)에 칼마·MDD로 이기고, 블록 P(K5)
#         상위3 적중 > 무작위(3/29≈0.103)인가
#    ①②를 둘 다 만족하지 못하면 `INDUSTRY_LAYER_FROZEN=True`(13g·13p·16 진단 시트는 유지, 배분·격자만
#    생략 — 실행시간 ≈40분 절약)로 되돌리고 산업 배분 격자 라운드를 멈춘다(사용자 승인 필요 — REPORT50 §6
#    질문 1·2). 동결 되돌리기: `i_overrides={"INDUSTRY_LAYER_FROZEN": False}`.
#
#  ---- 이전 라운드 레시피(참고) ----
#  VERSION: v1.6.0 - 2026-09-13 - [문서 + 실행 레시피 — 실행 로직 무변경] S v0.46.0 · I v0.10.0 · M v1.53.1.
#                    REPORT49 구현분: H1 ★★ 독립 산업 슬리브 · H2 산업 13g 풀링 진단 5열 ·
#                    H3 within-parent 격하 · H4 13p 블록 E(격자 변형별) · H5 [대피처격자] 퇴역 ·
#                    H7 M 00시트 버전 문자열.
#
#  ★ 이번 라운드의 본체는 **산업 계층의 구조 전환(H1)**이다. 지금까지 산업은 "S★가 준 섹터 비중 **안에서**
#    부모 ETF를 대체"하는 새장이었고(S★가 XLK에 90%를 주므로 무대는 사실상 'SOXX vs XLK' 하나),
#    그래서 세 라운드 연속 배분 0일이었다. 이제 산업에 **자체 예산 f × S★ 총노출**을 주고 29산업을
#    **부모 무관 공동 순위**로 고른다. ⚠ 라이브는 꺼져 있어(f=0.0) I★는 v0.9.0과 비트 동일 — 격자만 늘어난다.
#    실행은 한 번이면 된다(⚠ 신호·사이징 기본값 변경 0건):  out = RP.main()
#
#  ★ 이번 실행에서 볼 것(판정 기준선은 리포트45/8 실측 + REPORT49 §3.4 근사):
#    1) **H1 — 13_산업배분전략 [산업슬리브격자] 4행 + [산업슬리브격자·대조] 6행.**
#       ⚠ **대조군과 짝지어 읽는 것이 전부다.** 같은 f를 SPY에 넣은 행보다 칼마가 높고 MDD가 낮아야 채택
#       후보다 — 산업 슬리브의 값은 '수익'이 아니라 '★와 다른 것을 든다'(일간 상관 0.63)에서 온다.
#       REPORT49 §3.4 근사 기준선: f=0.3 → 칼마 3.06 vs SPY 혼합 대조 2.35 · MDD −9.6% vs −12.1%.
#       (실제 엔진 값은 다를 수 있다 — 방향만 같으면 된다. ★ 재현이 31.4%였던 근사라서.)
#       로그: event=industry_sleeve_grid share=… k=… holdings=GDX(XLB)…;XME(XLB)… xlk_share=…
#             **xlk_share가 크면 S★ XLK 편향의 재탕**이지 새 정보가 아니다(REPORT49 §3.4 실측 0.189).
#    2) **H1 판정 ② — 13p 블록 P(풀링 슬리브 소수클래스).**
#       '슬리브 → 실현 상위k 비율'이 무작위(k/N ≈ 0.103)를 넘어야 한다. 넘지 못하면 CAGR이 좋아도
#       그건 분산 효과일 뿐이므로 채택하지 않는다(REPORT49 §3.3: 풀링 상위3 적중 0.17 — 넘긴 하지만 약하다).
#    3) **H2 — 13g 풀링 진단 5열, 특히 '풀링 사전방향 판정'.**
#       v0.9.0 I-F가 연 P_REL_VOL_RATIO는 산업 풀링에서 **부호가 뒤집힌 신호**다(저변동 1위의 상위3 적중
#       0.050 · vs SPY −0.87%/21일 t −4.1) — 그래서 졌다. 이번 실행에서 그 신호가 **'반전(사전방향과 반대)'**
#       으로 찍히는지 확인할 것. 찍히면 H2가 제 일을 한 것이고, 앞으로 그런 신호는 채택 전에 걸러진다.
#       로그: event=within_parent_select … pooled_reversed=P_REL_VOL_RATIO,…
#    4) **H4 — 13p 블록 E(격자 변형별 소수 클래스).**
#       리포트45에서 [단독회피격자]는 CAGR로만 기각됐고 '회피 판단이 무작위를 넘었는가'는 **잴 수 없었다**
#       (블록 B가 라이브 배분만 세는데 라이브는 회피 0일). 이제 블록 E가 격자 행의 회피일·적중·집중도를 낸다.
#       볼 것: [단독회피격자] 행의 '회피 섹터 → 실현 하위3'이 무작위(0.273)를 넘는가, '최빈 회피(집중도)'가
#       작은가. 넘고 분산돼 있으면 **CAGR이 져도 '꼴찌 예측은 성립'으로 기록**하고 다음 설계에 쓴다.
#    5) **H5 확인** — 13 시트에 [대피처격자] 행이 **없어야** 한다(격자 38 → 33행 안팎). 13p 블록 D·13m 블록 D는 유지.
#    6) **H3 확인** — 13_산업배분전략이 ★ + 대조군 + [산업슬리브격자]만 남아 짧아진다(within-parent 28행 생략).
#       ⚠ 계산 생략일 뿐이라 **I★ 성과는 무변경**이어야 한다.
#    7) **H7 확인** — market_regime_report 00시트 '버전' 줄이 **v1.53.1**로 찍힌다(리포트66은 v1.51.0로 틀렸다).
#    8) **★ 성과 불변** — S★ CAGR 35.87% / 샤프 2.213 / MDD −9.55%, I★ 동일. 어긋나면 즉시 알려 줄 것.
#
#  ⚠ v0.46.0 / v0.10.0 되돌리기 한 줄:
#    i_overrides={"INDUSTRY_SLEEVE_GRID": ()}                           # H1 슬리브 격자 끄기
#    i_overrides={"INDUSTRY_SLEEVE_SHARE": 0.3, "INDUSTRY_SLEEVE_K": 3} # ⚠⚠ H1 라이브 전환(판정 통과 뒤에만)
#    i_overrides={"INDUSTRY_GRID": True}                                # H3 within-parent 격자 복원
#    i_overrides={"INDUSTRY_LEADER_STANDALONE_GRID": (0.03, 0.07)}      # I-F 격자 복원(권하지 않음 — 반전 신호)
#    s_overrides={"ROTATION_SHELTER_RULE_GRID": ("votes2","votes2_nonup","leader_only")}   # H5 복원
#    s_overrides={"GRID_CRITERION_PRIMARY_BIAS": False}                 # 격자 ⑤ 끄기(종전 4기준)
#
#  ---- 이전 라운드 레시피(참고) ----
#  VERSION: v1.5.0 - 2026-09-13 - [문서 + 단계별 실행 레시피 — 실행 로직 무변경] S v0.45.0 · I v0.9.0 · M v1.53.0.
#                    REPORT48 구현분: G1 격자 ⑤ 버그수정 · G2 13p 블록 D(득표×국면) · G3 [대피처격자] ·
#                    G4′ 유효표본 게이트 사전등록(판독서 G4는 철회) · G5 [단독회피격자] · G6 F1 퇴역 ·
#                    G7 [국면상속격자] 퇴역 · G9 M 리포트 동반 업로드.
#                    + I-F(사용자 지시 "왜 industry는 수정 안했어?"): 산업 [단독리더격자] — 산업 배분이
#                      세 라운드 연속 0일인 원인이 신호 품질이 아니라 **정족수**였다(채택 0~1개 vs 교차확인 2).
#                      순서 잣대 여유로 교차확인을 면제하는 격자. 라이브는 꺼짐 → I★ 비트 동일.
#
#  ★ 이번 라운드는 ⚠ 기본값(신호·채택·사이징) 변경이 **0건**이다 — 라이브 ★는 v0.44.0과 비트 동일하고,
#    늘어난 것은 13 시트의 격자 행과 13p/13m/13f/00의 진단뿐이다. 그래서 단계를 쪼갤 필요 없이 한 번 돌린다:
#        out = RP.main()
#    그리고 **market_regime_report.xlsx도 같이 업로드**할 것(G9) — F5(M 13p)가 두 라운드째 미판정이다.
#
#  ★ 이번 실행에서 볼 것(판정 기준선은 리포트44 실측):
#    1) **G1 — 00시트 '격자 수렴 상태(①②③④⑤)' 줄.**
#       v0.44.0은 ★ 라벨을 alloc에서 찾았는데 그 키는 alloc["diag"] 안에 있어 ⑤가 **한 번도 작동하지 않았다**
#       (리포트44: "⑤ 통과 38/38" = 전부 통과가 아니라 전부 미검사). 이번 실행에서
#         · "⑤ 통과 n"이 **38 미만**이어야 하고
#         · "⑤에서 걸러진 행"에 [상한격자] XLK 100%·M헤어컷 E9/E11 같은 '주력을 더·오래' 행이 나와야 하며
#         · 13_섹터배분전략에 **'주력 평균비중' 열**이 새로 보여야 한다.
#       끄기(종전 4기준): s_overrides={"GRID_CRITERION_PRIMARY_BIAS": False}
#    2) **G2 — 13p 블록 D(득표 × M 국면)** · 00시트 '소수 클래스 정확도' 줄 끝 · 13f ⑪.
#       리포트44 재계산 기준선: 0표 0.182 / 1표 0.296 / 2표 0.396 / 3표 0.821 ·
#       득표≥2·중립 0.565 vs 상승 0.381 · **득표≤1·상승 0.227(초과 −0.71%/21일, 644일)**.
#       이 표가 [대피처격자] 판정의 근거다 — 득표≤1·상승이 무작위(0.27) 아래면 현행 슬리브 규칙이 손해다.
#    3) **G3 — 13_섹터배분전략 [대피처격자] 3행 + 13m 블록 D.**
#       기준선(리포트44 13m): 슬리브 실제 배분 10.64% vs 비주력 균등 10.38%(1,507일) — 현행 판단이 균등과 같다.
#       판정: 격자 4기준(⑤ 포함) **그리고** 13m 블록 D에서 그 규칙의 누적 기여가 '비주력 균등'을 넘는가.
#       CAGR 기대치는 작다(누적 +1.4%p 안팎) — 목적은 수익이 아니라 '판단을 정보 있는 날로 제한'이다.
#       끄기: s_overrides={"ROTATION_SHELTER_RULE_GRID": ()}
#    4) **G5 — [단독회피격자] + 13g '단독회피 자격(순서)' 열 + 13p ⑧.**
#       ⚠ 사전 예상 정정: 리포트44 13g 값으로 미리 돌려 보니 "order" 자격이 **2021·2023·2024·2026** 네 해에
#       선다(판독서는 2024·2026만 예상했다). 2021·2023은 모멘텀 계열의 '최근 252일 꼴찌→하위3'이 0.44~0.63인데
#       그 창은 **2020년 XLE가 1년 내내 바닥이던 구간**이다 — REPORT48 §2.3이 "한 섹터 고정이면 순환매 예측이
#       아니다"라고 배제한 바로 그 인공물이다. 문턱은 사후 적합을 피하려 **그대로 두고**, 대신 로그에
#       집중도 진단을 넣었다:
#         event=avoid_standalone_grid variant=order ... avoid_sectors=XLE=203;... top_share=0.62
#       **top_share가 크면 그 해는 '그 섹터를 빼는 규칙'**이다. 판정은 13p ⑧(발동일 꼴찌→실현 하위3이
#       무작위 0.27을 넘는가)과 top_share를 **같이** 본다. 넘지 못하면 하위 쪽은 이 시스템에서 닫는다.
#       끄기: s_overrides={"ROTATION_AVOID_STANDALONE_GRID": ()}
#    5) **G6·G7 퇴역 확인** — 13 시트에 [역방향회피격자]·[국면상속격자] 행이 **없어야** 한다(격자 38 → 33행 안팎).
#       13g '역방향 회피 자격' 열과 13p 블록 A 'M상속' 6열은 진단으로 그대로 남는다.
#    6) **I-F(산업) — 13_산업배분전략 [단독리더격자] 2행 + 13g '단독리더 여유(순서−무작위)' 열.**
#       배경: 산업 13g 연도별 채택 신호 수가 2018:1 2019:1 2020:0 2021:1 2022:1 2023:0 2024:0 2025:0 2026:1로
#       0~1개뿐이라 교차확인(ROTATION_MIN_AGREE=2)이 구조적으로 불가능했고, 그래서 산업 배분이 세 라운드
#       연속 **0일**이며 13p 블록 B가 **1행**(잴 것이 없음)이었다.
#       채택 신호 P_REL_VOL_RATIO의 순서 잣대 여유는 다섯 해 모두 +0.069~+0.101(무작위 0.327 대비)이다.
#       **판정은 CAGR이 아니다** — 13p 블록 B('부모 안 리더 → 부모 안 실현 1위')가 무작위(1/부모안 산업수)를
#       넘느냐, 그리고 로그 top_share(한 산업 쏠림)가 작으냐를 본다:
#         event=leader_standalone_grid edge=0.03 standalone_leader_days=… leaders=SOXX=…;… top_share=…
#       솔직한 기대치는 낮다(REPORT48 §5: 평가창 실현 적중 0.35 vs 무작위 픽 0.33 · 부모 초과 t ≥2 없음).
#       그래도 재는 이유는 **0일이면 영원히 판정이 안 나기 때문**이다 — 못 넘으면 그때 근거를 갖고 닫는다.
#       끄기: i_overrides={"INDUSTRY_LEADER_STANDALONE_GRID": ()}
#    7) **G4′** — 13g '유효표본 n_eff' 열을 볼 것. 2019 REL_DD_252H가 427.7(채택 33행 중 최소, 차순위 802.8)이다.
#       ⚠ 다만 **빼도 2019는 나아지지 않는다**(원자료 재검: 상위3 0.198→0.202, 하위3 0.294→0.397,
#       초과 −0.37→−0.72%) — 판독서 G4(최소 학습창)는 그래서 철회했다. 굳이 재 보려면:
#         s_overrides={"ROTATION_MIN_N_EFF": 600.0}
#
#  ⚠ v0.45.0 되돌리기 한 줄(전부 격자·진단이라 ★ 성과에는 닿지 않는다):
#    s_overrides={"ROTATION_SHELTER_RULE_GRID": ()}                     # G3 대피처 격자 끄기
#    s_overrides={"ROTATION_SHELTER_RULE": "votes2"}                    # ⚠ G3 라이브 전환(4기준 통과 뒤에만)
#    s_overrides={"ROTATION_AVOID_STANDALONE_GRID": ()}                 # G5 단독회피 격자 끄기
#    s_overrides={"ROTATION_AVOID_STANDALONE_HIT": (0.40, 0.45)}        # G5 문턱 올리기(인공물 배제 실험)
#    s_overrides={"ROTATION_REVERSE_AVOID_GRID": (2.0, 2.5)}            # G6 부활(권하지 않음 — 근거가 뒤집혔다)
#    s_overrides={"ROTATION_REGIME_INHERIT_GRID": ("m_off", "m_up")}    # G7 부활
#    s_overrides={"GRID_CRITERION_PRIMARY_BIAS": False}                 # G1 ⑤ 끄기(종전 4기준)
#    s_overrides={"ROTATION_MIN_N_EFF": 600.0}                          # G4′ 유효표본 게이트 켜기
#    i_overrides={"INDUSTRY_LEADER_STANDALONE_GRID": ()}                # I-F 단독리더 격자 끄기
#    i_overrides={"INDUSTRY_LEADER_STANDALONE_EDGE": 0.03}              # ⚠ I-F 라이브 전환(4기준 통과 뒤에만)
#
#  ---- 이전 라운드 레시피(참고) ----
#  VERSION: v1.4.0 - 2026-09-13 - [문서 + 단계별 실행 레시피 — 실행 로직 무변경] S v0.44.0 · I v0.8.0 · M v1.53.0.
#                    REPORT47 구현분: F1 역방향 회피 격자 부활 · F2 ⚠ 최선가용을 순서 잣대로 · F3 M 상속
#                    (13p 비교열 + [국면상속격자]) · F4 ★ 13q 하락확률보정 · F5 ★ M 13p · F6 산업 벤치 진단·격자 플래그 ·
#                    F7 격자 기준 ⑤(주력 편향).
#
#  ★ 단계별 실행 레시피(REPORT47 §7 — 한 번에 하나):
#    1) **F2 단독 판정**(⚠ 채택층): out = RP.main()
#       이번 기본 실행에서 ⚠는 F2 하나다(ROTATION_BEST_AVAILABLE_RANK_BY="hit"). 리포트43 13g로 재현한
#       영향 범위는 **2020년 한 해**뿐이다(엄격 신호가 없어 최선가용이 발동하는 유일한 해 중 선택이 갈리는 해):
#         v0.43 t 순 → REL_DD_252H(t 1.46·순서 0.309) + SCORE_CS_Z  → 리더 12일 · 초과 −0.7%p
#         v0.44 순서 순 → SCORE_PCT(0.319) + SCORE_CS_Z(= v0.42.1 조합) → 리더 161일 · 초과 +10.5%p(기대)
#       볼 것: 13p 블록 B **2020 리더 판단일·리더→상위3**(기준선 12일/0.083) · 13f ③(1.62 → 2.0 복귀하는가) ·
#              2026은 무변경이어야 한다(16일/1.000 유지 — 그 해는 최선가용이 발동하지 않는다).
#       되돌리기 대조: out = RP.main(s_overrides={"ROTATION_BEST_AVAILABLE_RANK_BY": "t"})
#    2) 같은 실행에서 함께 나오는 진단·격자(신호 무변경 — 따로 돌릴 필요 없다):
#       · 13p 블록 B '역방향 회피 대상 → 실현 상위3'(기준선: 상대변동성 최저 섹터 0.147) + 13_섹터배분전략
#         **[역방향회피격자] 2행** → 4기준(+⑤) 통과하면 그때 라이브 스위치를 켠다(ROTATION_REVERSE_AVOID).
#       · 13p 블록 A 'M상속(M현금/M상승아님) MCC'와 'M상속 우위(MCC차)'(기준선: 자기 0.055 · M현금 0.079 ·
#         M상승아님 0.088) + **[국면상속격자] 2행**.
#       · **13q_하락확률보정** — 신뢰도 기울기 ≥ 0.10이면 방향 정보가 있다는 뜻이고, 그때 비로소 문턱을
#         기저율에 맞춰 재현율 한계(라벨 방식 ~0.074)를 풀 수 있다. **배분 미사용**.
#       · M 리포트 **13p_소수클래스정확도**(기준선: SPY 현금 MCC +0.160 · 상승아님 +0.187).
#       · 격자 수렴 줄이 **①②③④⑤**로 — ⑤에서 걸러진 행(주력 편향을 키우는 행)을 함께 보여 준다.
#       · I 13g 진단 4열(중앙값 벤치 t · 부모 안 실현1위 비율) — 부모 초과 타깃의 구조적 편향(0.45~0.49)이
#         원인인지, 산업 순위 자체에 정보가 없는지를 가른다.
#    3) 그 다음 라운드 후보(각각 단독):
#       s_overrides={"ROTATION_STRICT_REQUIRE_UNIFORM_T": 1.0}      # R4 균등 t 하한
#       s_overrides={"SECTOR_HAZARD_CONFIRM_GATE": True}            # S-N
#       i_overrides={"ROTATION_VALIDATION_BENCH": "group_median"}   # F6(c) 타깃 전환
#
#  ⚠ v0.44.0/v0.8.0 기본값과 되돌리기 한 줄:
#    S: s_overrides={"ROTATION_BEST_AVAILABLE_RANK_BY": "t"}   # ⚠ F2 되돌리기(v0.43.0 동작)
#       s_overrides={"ROTATION_BEST_AVAILABLE_MIN_HIT": 0.27}  # F2-b 켜기(기본 None)
#       s_overrides={"ROTATION_REVERSE_AVOID_GRID": ()}        # F1 격자 끄기
#       s_overrides={"ROTATION_REVERSE_AVOID": True}           # ⚠ F1 라이브 적용(격자 통과 뒤에만)
#       s_overrides={"RUN_DOWN_PROB": False}                   # F4 13q 끄기(실행시간 절약)
#       s_overrides={"GRID_CRITERION_PRIMARY_BIAS": False}     # F7 끄기(종전 4기준)
#       s_overrides={"ROTATION_RECENT_MIN_T": None}            # ⚠ R2 되돌리기
#    I: i_overrides={"INDUSTRY_GRID": False}                   # F6(a) 격자 28행 생략(13·13c·14는 그대로)
#       i_overrides={"ROTATION_VALIDATION_BENCH": "group_median"}  # ⚠ F6(c) 검증 타깃 전환
#       i_overrides={"INDUSTRY_LAYER_FROZEN": True}            # ⚠ 동결(배분 시트 생략)
#
#  VERSION: v1.0.1 - 2026-09-12 - [문서만 변경 — 실행 로직·기본값 무변경] S v0.39.0에서 SECTOR_EXCLUDE
#                    기본값이 ("XLB","XLE") → ()(11섹터 전부 예측)로 돌아갔다(사용자 지시 "sector는 다시
#                    XLE, XLB 같이 예측"). 아래 사용 예시의 기본값 설명과 되돌리기 한 줄을 그에 맞게 고쳤다.
#                    main(sector_exclude=...)의 동작 자체는 그대로다(None이면 S 기본값 사용).
#  VERSION: v1.0.0 - 2026-09-12 - M(시장국면) → S(섹터) → I(산업) 한 번에 실행하는 노트북 셀용 러너.
#                    Colab / Kaggle / 로컬 공용 — 실행 환경을 M.runtime_env()로 판별해 출력·캐시 경로를 정한다.
#
#  왜 별도 파일인가: 실행 셀에 흩어져 있던 "wget → 모듈 로드 → run → report → download"를 한 곳에 모아
#    (1) Kaggle에서는 /kaggle/working 아래(노트북 Persistence(Files) 설정 시 세션 간 유지, 커밋 시 Output 저장)에
#        리포트·캐시를 두고 날짜 폴더(reports/YYYY-MM-DD/)로 이력을 쌓는다 — 사용자 지시 "엑셀 결과도 kaggle
#        영구 저장소에 보관".
#    (2) Colab에서는 종전처럼 zip 1개로 자동 다운로드.
#    (3) 실행 끝에 **실매매 적용 전략 배너**(M ★ SPY 국면전략의 다음 거래일 목표비중·예상 행동)를 찍고,
#        S★·I★는 진단용임을 함께 표시한다 — 사용자 지시 "실제 매매에서 사용하는 전략이 뭔지 확실히 표시".
#    (4) 캐시가 세션 간 살아남으면(Kaggle Persistence) 두 번째 실행부터 S·I가 산업/섹터당 수 초로 끝난다.
#
#  사용(노트북 셀):
#      !wget -q -O run_pipeline.py https://raw.githubusercontent.com/yeomin1024/stock/main/run_pipeline.py
#      ... (market_regime_trader.py / sector_rotation.py / industry_rotation.py 도 같은 방식으로 받는다)
#      import run_pipeline as RP
#      out = RP.main()                                    # 기본: 11섹터 전부 예측(SECTOR_EXCLUDE=()), 산업 계층은
#                                                         #   v0.13.0부터 **해제**(배분·격자 시트 전부 산출) +
#                                                         #   산업 국면은 M 상속(INDUSTRY_REGIME_SOURCE="m_inherit")
#      out = RP.main(sector_exclude=("XLB", "XLE"))       # ⚠ 되돌리기: 9섹터로 다시 좁힌다(S v0.37~v0.38 상태)
#      out = RP.main(sector_exclude=("XLB",))             # ⚠ XLB만 제외(XLE는 예측)
#      out = RP.main(run_industry_layer=False)            # 산업 계층 자체를 생략(M+S만 — 리포트 2개)
#      out = RP.main(i_overrides={"INDUSTRY_TREND_GATE_GRID": ()})     # [리더추세게이트격자] 끔
#      out = RP.main(i_overrides={"TREND_GATE_SRC": "m63"})             # 추세 기준을 63일 모멘텀으로(기본 ext200)
#      out = RP.main(i_overrides={"SHOW_DECLINE_WARN_SUMMARY": False})  # 00·01Z의 산업별 하락경고 표시 끔
#      out = RP.main(i_overrides={"INDUSTRY_LEADER_BRAKE": True})      # ⚠⚠ 브레이크 다시 켜기(v0.15.0 동작 — 3기준 미달이었다)
#      out = RP.main(i_overrides={"ROTATION_LEADER_TOPK": 2})          # ⚠ 리더 분산을 라이브로(격자 판정 통과 후에만)
#      out = RP.main(i_overrides={"INDUSTRY_LEADER_TOPK_GRID": ()})    # [리더분산격자] 끔
#      out = RP.main(i_overrides={"INDUSTRY_WARN_CUT_GRID": ()})       # [하락경고격자] 끔
#      out = RP.main(i_overrides={"BRAKE_DD_Q": 0.05, "BRAKE_VOL_Q": 0.95})  # 브레이크를 더 엄격하게(제동일 감소)
#      out = RP.main(i_overrides={"BRAKE_USE_VOL": False})             # 낙폭 조건만 사용(변동성 조건 끔)
#      out = RP.main(i_overrides={"INDUSTRY_LEADER_BRAKE_GRID": ()})   # [리더위험격자]만 끔(라이브 브레이크는 유지)
#      out = RP.main(i_overrides={"ROTATION_ADOPT_BY": "t"})          # ⚠ P1 되돌리기(t 기준 채택 — v0.13.0 동작, 리더 0일로 복귀)
#      out = RP.main(i_overrides={"ROTATION_ORDER_T_FLOOR": 1.0})      # P1 안전판만: 순서로 고르되 t≥1.0 하한도 요구
#      out = RP.main(i_overrides={"ROTATION_ORDER_TOP_K": 4})          # 채택 폭 넓히기(교차확인 2는 그대로)
#      out = RP.main(i_overrides={"COUPLING_STATE": False})            # P2·P3 끄기(01_일별 2열·17 시트 생략)
#      out = RP.main(i_overrides={"INDUSTRY_REGIME_SOURCE": "own"})  # ⚠ N2 되돌리기(산업 자기 국면기계 — v0.12.0 동작)
#      out = RP.main(i_overrides={"INDUSTRY_LAYER_FROZEN": True})     # 동결(격자·13f·13j·13l·15만 생략 — 13·13c는 유지)
#
#  Kaggle 노트북 설정(오른쪽 패널): Internet = On, Persistence = "Files only"(또는 Variables & Files),
#    Accelerator = None(CPU 4코어면 fork 병렬 4워커). Environment는 최신(Pin to original 아님) 권장.
#
#  ※ 본 코드는 연구/교육용 도구이며 투자 자문이 아니다.
# =============================================================================
from __future__ import annotations

import os
import sys
import shutil
import time
import dataclasses
import datetime as dt
import importlib.util
from typing import Any, Dict, Optional, Tuple

VERSION = "v1.20.0"
VERSION_DATE = "2026-09-16"

MODULE_FILES = {
    "market_regime_trader": "market_regime_trader.py",
    "sector_rotation": "sector_rotation.py",
    "industry_rotation": "industry_rotation.py",
    "stock_regime": "stock_regime.py",          # [v1.16.0] 4번째 계층(K) — 개별 주식
}


def load_module(name: str, path: Optional[str] = None):
    """이전 세션에서 캐싱된 낡은 모듈을 버리고 파일에서 새로 로드한다. ★ exec 전에 sys.modules에 등록 —
    dataclass/피클/fork 자식이 모듈 이름으로 다시 찾을 때 실패하지 않게(사용자 셀의 기존 패턴 그대로)."""
    path = path or MODULE_FILES[name]
    if not os.path.exists(path):
        raise FileNotFoundError(f"{path} 가 없습니다 — 셀 상단의 wget(GitHub raw)이 실패했는지 확인하세요")
    sys.modules.pop(name, None)
    spec = importlib.util.spec_from_file_location(name, path)
    mod = importlib.util.module_from_spec(spec)
    sys.modules[name] = mod
    spec.loader.exec_module(mod)
    return mod


def detect_env(M=None) -> str:
    if M is not None and hasattr(M, "runtime_env"):
        return M.runtime_env()
    if os.environ.get("KAGGLE_KERNEL_RUN_TYPE") or os.path.isdir("/kaggle/working"):
        return "kaggle"
    try:
        import google.colab  # type: ignore  # noqa: F401
        return "colab"
    except Exception:
        return "local"


def base_dir_for(env: str) -> str:
    """리포트·캐시가 놓일 기준 폴더. Kaggle은 /kaggle/working(영구 저장 대상), Colab은 /content, 로컬은 cwd."""
    if env == "kaggle":
        return "/kaggle/working"
    if env == "colab" and os.path.isdir("/content"):
        return "/content"
    return os.getcwd()


def _banner(lines) -> None:
    width = max(len(l) for l in lines) + 4
    print("=" * width)
    for l in lines:
        print(f"  {l}")
    print("=" * width)


def main(sector_exclude: Optional[Tuple[str, ...]] = None, run_industry_layer: bool = True,
         run_stock_layer: bool = True,
         download: bool = True, keep_history: bool = True,
         m_overrides: Optional[Dict[str, Any]] = None, s_overrides: Optional[Dict[str, Any]] = None,
         i_overrides: Optional[Dict[str, Any]] = None,
         k_overrides: Optional[Dict[str, Any]] = None, base_dir: Optional[str] = None,
         _hooks: Optional[Dict[str, Any]] = None) -> Dict[str, Any]:
    """M → S → I 실행 + 리포트 + (Colab) 다운로드 / (Kaggle) 영구 보존 + 실매매 배너.
    sector_exclude: None이면 sector_rotation.py의 기본 그대로 — v0.39.0부터 기본은 ()(11섹터 전부 예측).
        ⚠ 9섹터로 되돌리려면 sector_exclude=("XLB","XLE"). 제외는 신호·배분·성과를 바꾸는 설정이다.
    *_overrides: 각 Config 필드 덮어쓰기(dataclasses.replace) — 예: s_overrides={"MAX_WORKERS": 2}.
    _hooks: 테스트 전용 — {"m_run": f(mcfg)->res, "s_run": f(res,M,scfg)->sres, "i_run": f(sres,res,M,S,icfg)->ires}
            (네트워크 없는 샌드박스에서 합성데이터로 러너 전체 경로를 검증하기 위한 주입점)."""
    t_all = time.time()
    M = load_module("market_regime_trader")
    S = load_module("sector_rotation")
    I = load_module("industry_rotation") if run_industry_layer else None
    # ---- [v1.16.1] K(주식 계층)를 **여기서 미리 로드**한다 ----
    #   왜 앞으로 옮겼나: v1.16.0은 K를 맨 끝에서 로드했고 배너에 K가 없었다. 그래서 (1) 배너만 보면
    #   주식 계층이 파이프라인에 없는 것처럼 보이고 (2) stock_regime.py를 wget 목록에 안 넣었으면
    #   **M·S·I를 20분 돌린 뒤에야** "파일 없음"을 알게 된다. 둘 다 사용자가 바로 겪은 문제다.
    #   이제 배너에 K 버전이 같이 찍히고, 파일이 없으면 **시작 5초 안에** 고치는 방법까지 알려 준다.
    #   ⚠ 여기서는 **로드만** 한다 — 실제 실행(다운로드·리포트)은 M·S·I 리포트가 끝난 뒤다.
    K, K_note = None, ""
    if run_stock_layer:
        _kp = MODULE_FILES["stock_regime"]
        if not os.path.exists(_kp):
            K_note = (f"⚠ {_kp} 없음 → 주식 계층 건너뜀. "
                      f"노트북 상단 wget 목록에 {_kp}를 추가하세요(M·S·I와 같은 GitHub raw 경로). "
                      f"의도한 것이면 main(run_stock_layer=False)로 명시하면 이 경고가 사라집니다")
        else:
            try:
                K = load_module("stock_regime")
                assert hasattr(K, "run") and hasattr(K, "build_report"), \
                    "stock_regime.py에 run/build_report가 없음 — 파일이 낡았는지 확인하세요"
            except Exception as e:
                K, K_note = None, f"⚠ 주식 계층 로드 실패({type(e).__name__}: {str(e)[:90]}) → 건너뜀"
    else:
        K_note = "주식 계층 끔(run_stock_layer=False)"
    print("M.VERSION:", getattr(M, "BUNDLE_VERSION", "??"), "| S.VERSION:", getattr(S, "VERSION", "??"),
          "| I.VERSION:", (getattr(I, "VERSION", "??") if I is not None else "(생략)"),
          "| K.VERSION:", (getattr(K, "VERSION", "??") if K is not None else "(생략)"),
          "| runner:", VERSION)
    if K_note:
        print(f"[runner] {K_note}")
    elif K is not None:
        print(f"[runner] 주식 계층 K 켜짐 — 대표 티커 {len(getattr(K, 'STOCK_UNIVERSE', {}))}종 · "
              f"리포트는 M·S·I가 끝난 **뒤** 마지막에 생성됩니다(펀더멘탈·어닝 다운로드가 있어 몇 분 걸립니다)")
    # ---- [v1.19.0 ★★ 신규] 구버전 파일 감지 — 사용자가 "왜 안 고쳤어"를 두 번 말하지 않도록 ----
    #   R61에서 실제로 일어난 일: 코드는 고쳐졌는데 노트북이 **GitHub의 이전 파일**을 wget해서
    #   S v0.50.0 / I v0.21.0 / K v0.2.1로 돌았다. 리포트에 00A 시트가 없고 비중 합계도 그대로였다.
    #   배너만 보고는 그것을 알 수 없었다 — 버전 숫자는 찍혔지만 **무엇이 있어야 하는지**가 없었다.
    #   ⇒ 이제 최소 버전을 코드가 알고 있고, 미달이면 **어느 파일을 갱신해야 하는지** 크게 알린다.
    _MIN = {"sector_rotation.py": ("S", "v0.56.0", S),
            "industry_rotation.py": ("I", "v0.25.0", I),
            "stock_regime.py": ("K", "v0.3.1", K)}
    def _vt(x):
        try:
            return tuple(int(p) for p in str(x).lstrip("v").split(".")[:3])
        except Exception:
            return (0, 0, 0)
    _stale = []
    for _fn, (_tag, _min, _mod) in _MIN.items():
        if _mod is None:
            continue
        _got = str(getattr(_mod, "VERSION", "v0.0.0"))
        if _vt(_got) < _vt(_min):
            _stale.append((_fn, _tag, _got, _min))
    print("[runner] ── 기능 점검(이 실행에 무엇이 들어 있나) ─────────────────────────")
    for _fn, (_tag, _min, _mod) in _MIN.items():
        if _mod is None:
            print(f"[runner]   {_tag} {_fn:22s} 없음(건너뜀)")
            continue
        _got = str(getattr(_mod, "VERSION", "?"))
        _ok = "OK " if _vt(_got) >= _vt(_min) else "⚠ 구버전"
        _feat = []
        # K의 00A와 배분층은 v0.3.0에 함께 들어갔으므로 build_allocation 유무로 함께 판정한다
        #   (K는 00A 함수를 자기가 갖지 않고 I의 것을 재사용하므로 hasattr로는 구분되지 않는다).
        if hasattr(_mod, "build_asset_return_compare") or (_tag == "K" and hasattr(_mod, "build_allocation")):
            _feat.append("00A_수익비교")
        if hasattr(_mod, "build_portfolio_segments"):
            _feat.append("19블록C")
        if _tag == "K" and hasattr(_mod, "build_allocation"):
            _feat.append("배분층(총합1.0)")
        # [v1.20.0 ROUND66] S의 이번 라운드 신규 2건을 이름으로 확인한다 — 사용자가 "뭘 고쳤는지
        #   모르겠고 수치가 그대로"라고 지적한 R64 사고의 재발 방지. 둘 중 하나라도 없으면 구버전이다.
        if hasattr(_mod, "market_exposure_floor"):
            _feat.append("★E_t바닥·퇴출(라이브)")
        if _tag == "S" and hasattr(_mod, "build_curve_compare"):
            _feat.append("★00B_곡선그래프")
        print(f"[runner]   {_tag} {_fn:22s} {_got:9s} (최소 {_min}) {_ok}"
              + (f" | {' · '.join(_feat)}" if _feat else " | ⚠ 신규 기능 없음"))
    if _stale:
        print("[runner] " + "=" * 74)
        print("[runner] ⚠⚠⚠ 구버전 파일로 실행 중입니다 — 새 시트·기능이 리포트에 나오지 않습니다")
        for _fn, _tag, _got, _min in _stale:
            print(f"[runner]     {_tag}: {_fn} = {_got}  →  {_min} 이상 필요")
        print("[runner]   원인: 노트북 상단 wget이 GitHub의 **이전 파일**을 가져왔습니다.")
        print("[runner]   조치: 위 파일을 저장소(main)에 덮어쓴 뒤 다시 실행하거나,")
        print("[runner]         Kaggle 세션의 .py 캐시를 지우고(런타임 재시작) wget을 다시 받으세요.")
        print("[runner]   확인: 실행 후 섹터 리포트 **2번째 탭이 00B_곡선그래프(실제 꺾은선 차트)**이고")
        print("[runner]         **22_시장예산바닥판정** 시트가 있으면 v0.56.0으로 갱신된 것입니다.")
        print("[runner] " + "=" * 74)
    else:
        print("[runner]   ★ 전부 최신 — 섹터 리포트 2번째 탭에 00B_곡선그래프(실제 그래프) · "
              "22_시장예산바닥판정이 나옵니다")
    assert hasattr(S, "run"), "S.run이 없음 - GitHub에 올린 sector_rotation.py를 다시 확인하세요"
    if I is not None:
        assert hasattr(I, "run"), "I.run이 없음 - GitHub에 올린 industry_rotation.py를 다시 확인하세요"

    env = detect_env(M)
    base = base_dir or base_dir_for(env)
    os.makedirs(base, exist_ok=True)
    print(f"[runner] 실행 환경 = {env} | 기준 폴더 = {base}")

    # ---- 경로 배치: 리포트·CSV·번들·캐시 전부 기준 폴더 아래(Kaggle이면 영구 저장 대상) ----
    mcfg = dataclasses.replace(
        M.CFG, CACHE_DIR=os.path.join(base, "cache_market_data"),
        OUT_XLSX=os.path.join(base, "market_regime_report.xlsx"),
        RESULT_BUNDLE_PATH=os.path.join(base, "market_regime_result.pkl.gz"),
        DAILY_CSV_PATH=os.path.join(base, "market_regime_daily.csv"),
        **(m_overrides or {}))
    s_kw = dict(CACHE_DIR=os.path.join(base, "cache_sector"),
                OUT_XLSX=os.path.join(base, "sector_regime_report.xlsx"),
                DAILY_CSV_PATH=os.path.join(base, "sector_regime_daily.csv"),
                ALLOC_CSV_PATH=os.path.join(base, "sector_allocation_daily.csv"))
    if sector_exclude is not None:
        s_kw["SECTOR_EXCLUDE"] = tuple(sector_exclude)
    s_kw.update(s_overrides or {})
    scfg = dataclasses.replace(S.CFG, **s_kw)
    icfg = None
    if I is not None:
        i_kw = dict(CACHE_DIR=os.path.join(base, "cache_industry"),
                    OUT_XLSX=os.path.join(base, "industry_regime_report.xlsx"),
                    DAILY_CSV_PATH=os.path.join(base, "industry_daily.csv"),
                    ALLOC_CSV_PATH=os.path.join(base, "industry_allocation_daily.csv"))
        i_kw.update(i_overrides or {})
        icfg = dataclasses.replace(I.CFG, **i_kw)
    print(f"[runner] 섹터 제외(SECTOR_EXCLUDE) = {tuple(getattr(scfg, 'SECTOR_EXCLUDE', ()) or ()) or '없음'}")

    # ---- 실행 ----
    hk = _hooks or {}
    res = (hk.get("m_run") or M.run)(mcfg)
    path = M.build_report(res, mcfg)
    sres = (hk.get("s_run") or S.run)(res, M, scfg)
    path2 = S.build_sector_report(sres, M=M)
    ires, path3 = None, None
    if I is not None:
        ires = (hk.get("i_run") or I.run)(sres, res, M, S, icfg)
        path3 = I.build_industry_report(ires, M=M, S=S)
    # ---- [v1.16.0] 4번째 계층 K(개별 주식) — 실패해도 M·S·I 리포트는 이미 만들어져 있다 ----
    #   [v1.16.1] 모듈은 위에서 이미 로드했다(배너에 버전이 찍혔고 파일 없음도 거기서 알렸다).
    kres, path4 = None, None
    if K is not None:
        try:
            print("[runner] 주식 계층(K) 시작 — 가격 + 펀더멘탈·어닝 다운로드(캐시 있으면 건너뜀)")
            # [v1.18.0] ★ I 계층의 **산업 ETF 비중**을 K에 넘긴다 — K의 [배분격자]
            #   'I계층 산업비중 연동' 행(4계층 정합의 본래 설계)이 이것 없이는 산출되지 않는다.
            _pw = None
            try:
                _ia = (ires or {}).get("alloc") or {}
                _itw = _ia.get("target_w")
                _icols = [c for c in (_ia.get("cols") or []) if _itw is not None and c in _itw.columns]
                if _itw is not None and _icols:
                    _pw = _itw[_icols].copy()
                    print(f"[runner]   K에 I 산업비중 전달 — {len(_icols)}산업 × {len(_pw)}일 "
                          f"(배분격자 '4계층 정합' 행이 살아납니다)")
                else:
                    print("[runner]   ⚠ I 산업비중이 비어 K 배분격자의 '4계층 정합' 행은 생략됩니다"
                          " (I 동결·배분 실패 시 정상)")
            except Exception as _e:
                print(f"[runner]   ⚠ I 산업비중 추출 실패({type(_e).__name__}) — K는 그 행만 빼고 진행합니다")
            kres = K.run(K.CFG, k_overrides, parent_w=_pw)
            path4 = K.build_report(kres, I=I)          # I를 넘기면 19·00A 시트가 함께 나온다
        except Exception as e:
            print(f"[runner] ⚠ 주식 계층(K) 실패 — M·S·I 리포트는 정상입니다: {type(e).__name__}: {e}")
            print("[runner]   다음 단계: (1) `pip install -q yfinance`로 설치 확인 "
                  "(2) stock_regime.py의 02_티커요약 '선택 항목'·'유효일 출처' 열로 필드 매칭 확인 "
                  "(3) 건너뛰려면 main(run_stock_layer=False)")
    paths = [p for p in (path, path2, path3, path4) if p and os.path.exists(p)]
    print("생성 완료:", " / ".join(paths))
    for _p in paths:                                    # 사용자 제약: 리포트 파일 전체 30MB 이하
        _mb = os.path.getsize(_p) / 1e6
        _flag = " ⚠ 30MB 초과" if _mb > 30.0 else ""
        print(f"[runner]   {os.path.basename(_p)} {_mb:.1f}MB{_flag}")

    # ---- 이력 보관(reports/YYYY-MM-DD/) — Kaggle Persistence·커밋 Output에 그대로 남는다 ----
    hist_dir = None
    if keep_history:
        stamp = str(res["cal"][-1].date()) if isinstance(res, dict) and "cal" in res else dt.date.today().isoformat()
        hist_dir = os.path.join(base, "reports", stamp)
        os.makedirs(hist_dir, exist_ok=True)
        for p in paths:
            shutil.copy2(p, os.path.join(hist_dir, os.path.basename(p)))
        for extra in (mcfg.DAILY_CSV_PATH, scfg.DAILY_CSV_PATH, scfg.ALLOC_CSV_PATH,
                      (icfg.DAILY_CSV_PATH if icfg else None), (icfg.ALLOC_CSV_PATH if icfg else None)):
            if extra and os.path.exists(extra):
                shutil.copy2(extra, os.path.join(hist_dir, os.path.basename(extra)))
        print(f"[runner] 이력 보관: {hist_dir}")

    # ---- 실매매 적용 전략 배너 ----
    try:
        nd = M.build_next_day_prediction(res, mcfg)
        lines = ["실매매 적용 전략 = ★ M: SPY 국면전략 (market_regime_report.xlsx)",
                 f"기준일 {nd['기준일'].date()}{nd.get('기준일_경과주의', '')}",
                 f"다음 거래일 {nd['다음거래일'].date()} : 확정 국면 {nd['확정국면']} / 목표비중 {nd['목표비중']:.2f} "
                 f"/ 현재 체결비중 {nd['체결비중']:.2f} / 예상 행동 {nd['예상행동_kr']}",
                 f"발동 규칙 {nd['발동규칙']} | 복합점수 백분위 {nd['복합점수백분위']} | H {nd['위험점수백분위(H)']}",
                 "S★(섹터)·I★(산업) 리포트는 진단·연구용 — 실매매 주문에 반영되지 않음"]
        _banner(lines)
    except Exception as e:  # noqa
        print(f"[runner] 배너 생성 실패(리포트는 정상): {type(e).__name__}: {e}")

    # ---- Colab 다운로드 / Kaggle 보존 안내 ----
    if download and env == "colab":
        if hasattr(M, "maybe_colab_download_many"):
            M.maybe_colab_download_many(paths, zip_name=os.path.join(base, "regime_reports_bundle.zip"))
        else:
            for p in paths:
                M.maybe_colab_download(p)
    elif env == "kaggle":
        print("[runner] Kaggle: 리포트는 /kaggle/working 에 보존됨 — 노트북 'Output' 탭에서 다운로드, "
              "Persistence(Files) 설정이면 다음 세션에도 캐시·리포트가 그대로 남음. 커밋(Save & Run All)하면 버전별 Output으로 저장.")
    print(f"[runner] 총 소요 {time.time() - t_all:.0f}초")
    return {"env": env, "base": base, "paths": paths, "history_dir": hist_dir, "res": res, "sres": sres, "ires": ires,
            "kres": kres, "mcfg": mcfg, "scfg": scfg, "icfg": icfg}


if __name__ == "__main__":
    main()
